# 测试环境定义格式

*页面ID: 139236* | *来源: Minecraft Wiki*

-{-|批次}-

* *测试环境定义文件**是用于定义游戏运行各个测试实例的世界测试环境的数据驱动文件。

# 定义格式

测试环境在游戏内使用<code>TEST_ENVIRONMENT</code>注册表，数据包路径为<code>test_environment</code>，即所有测试环境定义文件都需要在{{filepath|-{}-data/<*命名空间*>/test_environment}}目录内定义。

测试环境定义文件使用JSON格式，并具有下列结构：

<div class="treeview">
*  JSON文件根对象<section begin="codec_test_environment"/>
* * ：测试环境行为的类型。
* * 为<code>all_of</code>，则执行所有子测试环境中的行为。}}
* * ：所有子测试环境。游戏将按照此列表顺序从上到下依次执行其进入和退出时的行为。
* ** ：（命名空间ID或直接定义）与此结构相同，递归定义。
* * 为<code>function</code>，此测试环境会在进入和退出时运行指定的[函数](Java版函数.md)。}}
* * ：（命名空间ID）进入此测试环境时调用的函数。
* * ：（命名空间ID）退出此测试环境时调用的函数。
* * 为<code>game_rules</code>，此测试环境会在进入时设置指定的[游戏规则](游戏规则.md)，退出时重置指定的游戏规则为默认值。}}
* * ：设置游戏规则的值。
* ** ：一项游戏规则及其对应的值。
* * 为<code>weather</code>，此测试环境会在进入时设置指定的[天气](天气.md)，退出时重置天气循环。}}
* * ：设置为指定的天气。可以为<code>clear</code>（晴天）、<code>rain</code>（下雨）和<code>thunder</code>（雷暴）。
* * 为<code>time_of_day</code>/<code>clock_time</code>，此测试环境会在进入时设置时间为指定时间。}}
* * ：（命名空间ID）要设置的世界时钟。
* * ：（）设置当前/World Clock}}}}为指定时间。<section end="codec_test_environment"/>
</div>

# 定义行为

测试环境定义数据仅在服务端启动时被加载一次，使用命令不可以使测试环境定义被重新加载，而必须重启服务端。

测试环境是所有测试实例运行前进行检查的流程，所有测试实例必须在测试环境进入后才能进行测试。

游戏以批次运行测试实例，一个批次内的所有测试实例都会使用同一个测试环境。当游戏开始执行测试批次时，会读取当前批次要求的测试环境，并进入此测试环境；当此批次结束，准备运行下一批次前，游戏会检查下一批次的测试环境是否与结束批次的测试环境相一致（检查测试环境对象是否一致而非信息是否一致），如果不一致则退出上一个测试环境并进入下一个测试环境，否则不会退出当前测试环境而继续下一批次测试；当所有批次结束后，游戏退出最后一次进入的测试环境，测试结束。

# 历史

|
|
|：设置指定的布尔型游戏规则。
* ** ：一条布尔型游戏规则设置。
* *** ：游戏规则的精确ID。
* *** ：游戏规则的值。
* * ：设置指定的整数型游戏规则。
* ** ：一条整数型游戏规则设置。
* *** ：游戏规则的精确ID。
* *** ：游戏规则的值。
</div>
}}}}
|字段，现在设置世界时钟而非昼夜时间。}}
}}

# 导航

[Category:数据包](Category:数据包.md)

[de:Testumgebungsdefinition](de:Testumgebungsdefinition.md)
[en:Test environment definition](en:Test environment definition.md)
[fr:Définition d'environnement de test](fr:Définition d'environnement de test.md)
[pt:Definição do ambiente de teste](pt:Definição do ambiente de teste.md)