# 数据组件谓词

*页面ID: 124359* | *来源: Minecraft Wiki*

* *数据组件谓词（Data Component Predicate）**，也称**组件谓词（Component Predicate）**<ref></ref>，是用于测试[数据组件](数据组件.md)是否满足某种条件的谓词。

# 使用
游戏为数据组件谓词定义了多种格式，但不论是哪种格式，都需要指定数据组件谓词类型和谓词内容。本文以进度及谓词相关数据包文件中的格式展示语法。

# = 进度 =
在[进度谓词](进度定义格式.md)系统中，数据组件谓词可以用来测试物品、方块实体和实体的数据组件。其格式为多个<code>-{}-<*数据组件谓词ID*>: <*检测内容*></code>键值对的集合。

<div class="treeview">
*  准则谓词根节点
* *
* ** ：<code>-{}-<*检测内容*></code>
</div>

# = 命令 =
数据组件谓词可以在<code>minecraft:item_predicate</code>参数类型中测试物品，此参数类型的测试项可以是<code>-{}-<*数据组件谓词ID*>~<*检测内容*></code>。详见。

# = 物品模型 =
数据组件谓词可以在[物品模型映射](物品模型映射.md)中使用，为物品提供模型选择。此时谓词类型和谓词内容单独存在。

<div class="treeview">
*
* * ：<code>-{}-<*数据组件谓词ID*></code>
* * ：<code>-{}-<*检测内容*></code>
</div>

# 数据格式
数据组件谓词可以分为测试组件自身的存在性和测试组件的值是否满足某种条件两类谓词。

对于测试组件存在性的谓词，其标签名可以是所有数据组件的ID。由于目前数据组件谓词类型的ID和数据组件的ID重合，对于同名的谓词，游戏会优先将其推断为数据组件谓词类型的ID以测试具体的值，再尝试以数据组件的ID测试组件的存在性，而如果组件谓词本身无法接受这种格式则无法以这种格式测试组件的存在性。

<div class="treeview">
* ：空标签，测试此组件是否存在。
</div>

例如：<code>{"predicates": {"minecraft:instrument": {}}}</code>可以测试物品是否具有<code>instrument</code>组件，不需要关注此组件及其值是否能序列化，也不需要关注此组件的具体值。而<code>{"predicates": {"minecraft:potion_contents": {}}}</code>不允许，因为<code>potion_contents</code>是已有的数据组件谓词ID，因此无法测试存在性；而且此谓词也不接受这种格式。

游戏共定义了下列测试数据组件具体值的数据组件谓词：

# = attribute_modifiers =
检查<code>-{}-[attribute_modifiers](物品堆叠组件#attribute_modifiers.md)</code>组件中的属性修饰符。

<div class="treeview">
*
* * ：[属性修饰符](属性修饰符.md)的集合谓词。
* **
}}
</div>

# = bundle_contents =
检查<code>-{}-[bundle_contents](物品堆叠组件#bundle_contents.md)</code>组件中的物品堆叠。

<div class="treeview">
*
* * ：物品堆叠的集合谓词。
* **
}}
</div>

# = container =
检查<code>-{}-[container](物品堆叠组件#container.md)</code>组件中的物品堆叠。

<div class="treeview">
*
* * ：物品堆叠的集合谓词。
* **
}}
</div>

# = custom_data =
检查<code>-{}-[custom_data](物品堆叠组件#custom_data.md)</code>组件的自定义数据。

可以为一个JSON对象或NBT复合标签，也可以为字符串。如果是字符串，则将此字符串视为SNBT，游戏将在解析时自动将字符串转换为对应的NBT数据。

<div class="treeview">
* ：测试格式参见和。
</div>

# = damage =
检查<code>-{}-[damage](物品堆叠组件#damage.md)</code>和<code>-{}-[max_damage](物品堆叠组件#max_damage.md)</code>组件，即物品的[剩余耐久度或损坏值](耐久度.md)。损坏值来自<code>damage</code>组件，剩余耐久度来自<code>max_damage</code>组件减去<code>damage</code>组件。<code>max_damage</code>组件不存在时被视为0。

<div class="treeview">
*
* * ：检查物品的损坏值。可匹配单个整型值，也可匹配两个整型值所形成的闭区间。
* **
* * ：检查物品的剩余耐久度。可匹配单个整型值，也可匹配两个整型值所形成的闭区间。
* **
</div>

# = enchantments =
检查<code>-{}-[enchantments](物品堆叠组件#enchantments.md)</code>组件的魔咒。

<div class="treeview">
* ：条件的数组。其中的每一个条件都必须满足。
* * ：一个条件。
* ** ：要检查的魔咒。可以为单个魔咒ID、一个魔咒ID的列表或一个魔咒标签。若指定了多个魔咒，只需有其中一个魔咒存在即测试成功。
* ** ：检查魔咒的同时检查对应魔咒的等级。可匹配单个整型值，也可匹配两个整型值所形成的闭区间。若指定了多个魔咒，存在其中一个魔咒的等级符合即可成功。如果字段不存在，则该物品存在任一魔咒的等级符合即可成功。
* ***
</div>

# = firework_explosion =
检查<code>-{}-[firework_explosion](物品堆叠组件#firework_explosion.md)</code>组件的烟火之星爆裂效果。

<div class="treeview">
*
* *
}}
</div>

# = fireworks =
检查<code>-{}-[fireworks](物品堆叠组件#fireworks.md)</code>组件的烟花火箭数据。

<div class="treeview">
*
* * ：烟火之星爆裂效果的集合谓词。
* **
* * ：检测烟花火箭的飞行的时间，单位为“火药”（即表现为和在工作台上合成烟花火箭时所用的火药数相等）。可匹配单个整型值，也可匹配两个整型值所形成的闭区间。
* **
}}
</div>

# = jukebox_playable =
检查<code>-{}-[jukebox_playable](物品堆叠组件#jukebox_playable.md)</code>组件的[唱片](唱片.md)音乐信息。

<div class="treeview">
*
* * ：要测试的[唱片机曲目](唱片机曲目.md)。可以为一个唱片机曲目ID、一个唱片机曲目的列表或一个唱片机曲目标签。若指定的唱片机曲目包含物品的唱片机曲目，则测试成功。
</div>

# = potion_contents =
检查<code>-{}-[potion_contents](物品堆叠组件#potion_contents.md)</code>组件的药水效果。仅检查字段，其余数据不会被检测。

<div class="treeview">
* ：要测试的药水效果。可以为一个药水效果ID、一个药水效果的列表或一个药水效果标签。若指定的药水效果包含<code>potion_contents</code>组件的字段的药水效果，则测试成功。
</div>

# = stored_enchantments =
检查<code>-{}-[stored_enchantments](物品堆叠组件#stored_enchantments.md)</code>组件的魔咒。

<div class="treeview">
* ：条件的数组。其中的每一个条件都必须满足。
* * ：一个条件。
* ** ：要检查的魔咒。可以为单个魔咒ID、一个魔咒ID的列表或一个魔咒标签。若指定了多个魔咒，只需有其中一个魔咒存在即可测试成功。
* ** ：检查魔咒的同时检查对应魔咒的等级。可匹配单个整型值，也可匹配两个整型值所形成的闭区间。若指定了多个魔咒，存在其中一个魔咒的等级符合即可成功。如果字段不存在，则该物品存在任一魔咒的等级符合即可成功。
* ***
</div>

# = trim =
检查<code>-{}-[trim](物品堆叠组件#trim.md)</code>组件的[盔甲纹饰](盔甲纹饰.md)数据。此检查只检测命名空间ID，内联数据永远测试失败。

<div class="treeview">
*
* * ：要检查的盔甲纹饰材料。可以为单个盔甲纹饰材料ID、一个盔甲纹饰材料ID的列表或一个盔甲纹饰材料标签。若指定的盔甲纹饰材料包含所检测物品的盔甲纹饰材料，则测试成功。
* * ：要检查的盔甲纹饰图案。可以为单个盔甲纹饰图案ID、一个盔甲纹饰图案ID的列表或一个盔甲纹饰图案标签。若指定的盔甲纹饰图案包含所检测物品的盔甲纹饰图案，则测试成功。
</div>

# = villager/variant =

检查<code>-{}-[villager/variant](数据组件#villager/variant.md)</code>组件的村民类型。

<div class="treeview">
* ：要检查的村民类型。可以为单个村民类型ID、一个村民类型ID的列表或一个村民类型标签。若指定的村民类型包含所检测对象的村民类型，则测试成功。
</div>

# = writable_book_content =
检查<code>-{}-[writable_book_content](物品堆叠组件#writable_book_content.md)</code>组件的书与笔书页信息。若开启过滤，则以未过滤的文本原始信息为准。

<div class="treeview">
*
* * ：书页的集合谓词。
* **
</div>

# = written_book_content =
检查<code>-{}-[written_book_content](物品堆叠组件#written_book_content.md)</code>组件的成书数据。

<div class="treeview">
*
* * ：书页的集合谓词。检测书页信息，若开启过滤，则以未过滤的文本原始信息为准。
* **
* * ：完整匹配书的作者。
* * ：完整匹配书的标题，以未过滤的文本原始信息为准。
* * ：检测书是否为。可以为<code>0</code>（），<code>1</code>（），<code>2</code>（），<code>3</code>（）。可匹配单个整型值，也可匹配两个整型值所形成的闭区间。
* **
* * ：检测这本成书是否已经被解析，即检测组件中的值是否为<code>true</code>。
</div>

# 历史

|
|
|。}}
|
|。
|在<code>attribute_modifiers</code>中，用<code>id</code>取代了字段。}}
|
|
|
|
}}

# 参考

# 导航

[de:Gegenstands-Unterprädikate](de:Gegenstands-Unterprädikate.md)
[en:Data component predicate](en:Data component predicate.md)
[fr:Prédicat de composant de données](fr:Prédicat de composant de données.md)
[ja:データコンポーネントプレディケート](ja:データコンポーネントプレディケート.md)
[pt:Predicado de componente de dado](pt:Predicado de componente de dado.md)