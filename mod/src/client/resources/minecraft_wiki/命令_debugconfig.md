# 命令/debugconfig

*页面ID: 166841* | *来源: Minecraft Wiki*

调试配置阶段。

# 语法
   <code>debugconfig config <target></code>
  : 将玩家的网络阶段切换为配置阶段。

   <code>debugconfig dialog <target> <dialog></code>
  : 向网络阶段处于配置阶段的玩家发送对话框。

   <code>debugconfig unconfig <target></code>
  : 将网络状态处于配置阶段的玩家加入世界。

# 参数
（在<code>/debugconfig config</code>中）
   指定要切换到配置阶段的玩家。


（不在<code>/debugconfig config</code>中）
   指定一个玩家UUID，将尝试根据此UUID解析为在配置阶段中的玩家。


   指定要展示的对话框。由于网络阶段为配置阶段，事实上只有内联定义的对话框才可被解析。


# 结果

# 输出

# 历史

|，仅在IDE中可用。}}
|子命令。}}
|命令在启用[调试工具<code>MC_DEBUG_DEV_COMMANDS</code>](调试工具#DEV_COMMANDS.md)时可用。}}
}}

# 导航

[de:Befehl/debugconfig](de:Befehl/debugconfig.md)
[en:Commands/debugconfig](en:Commands/debugconfig.md)