# 服务端管理协议

*页面ID: 164462* | *来源: Minecraft Wiki*

中基于JSON-RPC协议，用于服务端管理的API|中基于RCON协议，用于远程执行命令的API|远程控制台协议}}

* *服务端管理协议（Server Management Protocol）**是用于管理服务端而开放的API。

# 配置
服务端管理协议默认关闭，必须设置[服务端配置文件](服务端配置文件.md)中的<code>management-server-enabled</code>为<samp>true</samp>才可以开启。

在服务端配置文件中，下列配置与服务端管理协议有关：
* <code>management-server-enabled</code>：（默认为<code>false</code>）是否开启服务端管理协议。
* <code>management-server-host</code>：（默认为<code>localhost</code>）服务端管理协议绑定的主机名。
* <code>management-server-port</code>：（默认为0）服务端管理协议监听的端口。如果此值为0，游戏将绑定当前未被占用的可绑定端口。
* <code>management-server-secret</code>：（默认为随机生成的有效令牌）用于认证的令牌，必须为只包含大写字母、小写字母及数字的长度为40的字符串（即满足正则表达式<code>^[a-zA-Z0-9]{40}$</code>）。
* <code>management-server-allowed-origins</code>：在使用浏览器WebSocket API的情况下，服务端管理协议允许连入的来源<ref>https://developer.mozilla.org/zh-CN/docs/Glossary/Origin</ref>。以逗号（<code>,</code>）分割，且不允许有前后空格。
* <code>management-server-tls-enabled</code>：（默认为<code>true</code>）是否启用TLS。
* <code>management-server-tls-keystore</code>：保存服务端用于TLS的私钥和证书的KeyStore。
* <code>management-server-tls-keystore-password</code>：KeyStore的密码。
* <code>status-heartbeat-interval</code>：（默认为0）控制管理服务器向已连接的客户端发送心跳通知的间隔，单位为秒。设置为0会禁用此功能。

如果服务端启动时不能绑定指定的主机名和端口，服务端将启动失败。

# 协议内容
服务端管理协议基于[WebSocket](wzh:WebSocket.md)（仅支持HTTP/1.1）和[JSON-RPC](#JSON-RPC.md) 2.0协议进行双向通信。

# = 认证与安全 =
服务端管理协议要求客户端连入时必须携带认证数据。游戏要求客户端建立WebSocket连接时，请求中需要包含<code>Authorization</code><ref>可参照[RFC2616，14.8节](https://datatracker.ietf.org/doc/html/rfc2616#section-14.8)及[MDN文档](https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Reference/Headers/Authorization)</ref>或<code>Sec-WebSocket-Protocol</code><ref>可参照[RFC6455，1.9节](https://datatracker.ietf.org/doc/html/rfc6455#section-1.9)及[MDN文档](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Sec-WebSocket-Protocol)</ref>认证请求头：
* 对于<code>Authorization</code>请求头，游戏要求输入格式为<code>-{}-Bearer <*认证令牌*></code>。
* 对于<code>Sec-WebSocket-Protocol</code>，游戏要求输入格式为<code>-{}-minecraft-v1, <*认证令牌*></code>。此格式用于浏览器WebSocket API<ref>https://developer.mozilla.org/zh-CN/docs/Web/API/WebSocket</ref>认证。

如果客户端连入时不带有有效的认证请求头，或认证令牌不相符，游戏会直接返回HTTP状态码401（Unauthorized）中止连接。对于浏览器WebSocket API认证方式，游戏还将验证连接来源（请求头<code>Origin</code><ref>可参照[RFC6454，7章](https://datatracker.ietf.org/doc/html/rfc6454#section-7)及[MDN文档](https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Reference/Headers/Origin)</ref>）：如果来源不在<code>management-server-allowed-origins</code>中列出，游戏也将以HTTP状态码401中止连接。

假设服务端管理协议绑定在40745端口，且认证令牌设置为<code>n2pQcIG1OQ92jot2xG1M0aw0ZWnrh4F3Z3jw8qRP</code>。此处使用[websocat](https://github.com/vi/websocat)进行举例说明，连接请求应如下所示：

<syntaxhighlight lang="shell">
$ websocat ws://localhost:40745
websocat: WebSocketError: WebSocketError: Received unexpected status code (401 Unauthorized)
websocat: error running
$ websocat ws://localhost:40745 -H 'Authorization: Bearer n2pQcIG1OQ92jot2xG1M0aw0ZWnrh4F3Z3jw8qRP'
{"jsonrpc":"2.0","method":"server/status","id":0}
{"jsonrpc":"2.0","id":0,"result":{"started":true,"version":{"name":"25w37a","protocol":1073742092}}}
</syntaxhighlight>

使用浏览器WebSocket API则如下所示。部分网页可能具有内容安全策略（CSP）<ref>https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Guides/CSP</ref>从而阻止不安全的WebSocket连接或跨域连接，因此此脚本不能在所有网页或控制台内生效。启用TLS（见下文）可解决不安全WebSocket问题，但仍不能绕过CSP的跨域限制。

<syntaxhighlight lang="javascript">
const socket = new WebSocket(
	'ws://localhost:40745',
    ['minecraft-v1', 'n2pQcIG1OQ92jot2xG1M0aw0ZWnrh4F3Z3jw8qRP']
);
socket.onmessage = console.log;
</syntaxhighlight>

同时，服务端管理协议还可以设置使用TLS（Transport Layer Security，传输层安全性协议）以提高整个协议的安全性。TLS默认开启，但服务端默认不设置相应的KeyStore文件和密码。

游戏初始化服务端管理协议时，如果使用TLS，游戏会按照下列流程逐步获取基础信息：
# 游戏首先读取<code>management-server-tls-keystore</code>配置中指定的KeyStore。
# * 如果文件未设置，游戏报错<code>TLS is enabled but keystore is not configured</code>。
# * 如果文件不存在，或不是一个文件，则报错<code>Supplied keystore is not a file or does not exist: <*文件*></code>。
# 读取KeyStore密码：
# # 首先读取环境变量<code>MINECRAFT_MANAGEMENT_TLS_KEYSTORE_PASSWORD</code>，如果此值不存在，则进行下一步。
# # 其次读取系统属性<code>management.tls.keystore.password</code>，如果此值不存在，则进行下一步。
# # 最后读取<code>management-server-tls-keystore-password</code>配置。
# # 如果上述3个步骤均未获得密码，游戏报错<code>Keystore password not set</code>。

如果文件和密码都设置且有效，游戏会以[PKCS #12](wzh:PKCS 12.md)格式读取KeyStore，获取其中的服务端证书和私钥。如果证书和私钥无效，在服务端初始化时将直接报错。任何在TLS初始化过程中的报错均会停止服务端。

服务端本身不会检验TLS证书的可信性，但客户端可能会检查。例如使用自簽章证书时客户端将报错：

{{syntaxhighlight|lang=shell|<nowiki>
$ websocat wss://localhost:40745 -H 'Authorization: Bearer n2pQcIG1OQ92jot2xG1M0aw0ZWnrh4F3Z3jw8qRP'
websocat: WebSocketError: WebSocket SSL error: error:0A000086:SSL routines:tls_post_process_server_certificate:certificate verify failed:ssl/statem/statem_clnt.c:2123: (self-signed certificate)
websocat: error running
$ websocat wss://localhost:40745 -H 'Authorization: Bearer n2pQcIG1OQ92jot2xG1M0aw0ZWnrh4F3Z3jw8qRP' --insecure # 不验证证书可信性
{"jsonrpc":"2.0","method":"server/status","id":0}
{"jsonrpc":"2.0","id":0,"result":{"started":true,"version":{"name":"25w37a","protocol":1073742092}}}
</nowiki>}}

# = JSON-RPC =
* *[JSON-RPC](wzh:JSON-RPC.md)（JSON-Remote Procedure Call）**是基于[JSON](JSON.md)的一种轻量级的远程过程调用协议。<ref>https://www.jsonrpc.org/</ref>

Minecraft中的实现与原版规范有较大差异，本章节主要描述Minecraft中的JSON-RPC协议规范。

客户端负责发起请求和处理响应，服务端负责处理请求和发送响应。服务端管理协议支持双向通信，因此websocat与Minecraft服务端既充当了客户端，也充当了服务端。在客户端和服务端的数据互换过程中，成员是区分大小写的。

  <span class="minetip" title="此项为必选项" style="color: red;">*</span>意味着websocat向Minecraft服务端发送数据时，Minecraft要求成员必须存在。
  <span class="minetip" title="存储时必存在" style="color: blue;">*</span>意味着Minecraft服务端向websocat发送数据时，成员一定存在。

在服务端管理协议中，所有的请求方法名称均为有效的[命名空间ID](命名空间ID.md)。所有以<code>minecraft:notification/</code>作为前缀的ID都是服务端发送到客户端的通知，而其他的ID均为客户端发送到服务端的请求。下文中，对于请求省略<code>minecraft</code>命名空间前缀，对于通知省略<code>minecraft:notification/</code>前缀。

所有请求或通知的参数均作为第一个方法传参传入，不具有名称。以下为示例（右箭头代表向服务端的信息，左箭头反之）：

<syntaxhighlight lang="shell">
-> {"jsonrpc":"2.0","method":"minecraft:allowlist/set","params":[{"name":"jeb_"}]({"name":"jeb_"}.md),"id":1}
<- {"jsonrpc":"2.0","id":1,"result":[{"id":"853c80ef-3c37-49fd-aa49-938b674adae6","name":"jeb_"}]}
<- {"jsonrpc":"2.0","method":"minecraft:notification/allowlist/added","params":[{"id":"853c80ef-3c37-49fd-aa49-938b674adae6","name":"jeb_"}]}
-> {"jsonrpc":"2.0","method":"minecraft:allowlist","id":2}
<- {"jsonrpc":"2.0","id":2,"result":[{"id":"853c80ef-3c37-49fd-aa49-938b674adae6","name":"jeb_"}]}
</syntaxhighlight>

下列为游戏对JSON-RPC协议的具体实现。

# == 请求 ==
每个RPC调用都对应一个**请求（Request）**。

<div class="treeview">
*  JSON元素根标签
* * ：使用的JSON-RPC协议版本，严格等于<code>2.0</code>。
* * ：由客户端建立的标识符，需要包含一个数字，否则视为[通知](#通知.md)。
* * ：包含要调用的方法的名称。
* * ：在调用方法时使用的参数值负载。
* ** ，则直接传递参数：}}
* ** ：单个参数，具体负载的数据类型见下文。
* ** }}，则按名称传递参数：}}
* ** ：一项参数名及对应的负载。
* *** ：单个参数，具体负载的数据类型见下文。
</div>

下文中，各个负载的参数名称会写在其参数后，例如<code>allowlist/set</code>负载的数据类型为数组，参数名称为<code>players</code>，那么会表示成 <code>players</code>。

尽管缺失元素时游戏可以正常解析，但并不会有效执行请求，返回的通知也仅为空数组。

# === 通知 ===
* *通知（Notification）**是<code>id</code>成员无效的请求。通知表明了客户端对相应的响应对象不感兴趣。因此，无论成功或失败，服务端都不会向客户端返回响应对象。

# == 响应 ==
当调用RPC时，除非请求是通知，服务器必须发送一个**响应（Response）**。

# === 成功 ===
<div class="treeview">
*  JSON元素根标签
* * ：使用的JSON-RPC协议版本，严格等于"2.0"。
* * ：与请求中<code>id</code>成员相同。
* * ：具体内容，取决于调用的方法。
</div>

# === 失败 ===
<div class="treeview">
*  JSON元素根标签
* * ：使用的JSON-RPC协议版本，严格等于"2.0"。
* * ：与请求中<code>id</code>成员相同。
* * ：错误对象。
* ** ：错误代码，表示发生的错误类型。
* ** ：错误消息，一条单句。
* ** ：详细的错误信息。
</div>

以下是常见的错误列表：

# == 请求 ==
Minecraft的JSON-RPC实现可以正确识别请求格式并返回相应结果。

的格式为多个请求组成的数组，游戏将按顺序进行解析并执行。以下为示例：

<syntaxhighlight lang="shell">
-> [{"jsonrpc":"2.0","id":1,"method":"minecraft:allowlist/set","params":[{"name":"jeb_"}]({"name":"jeb_"}.md)},{"jsonrpc":"2.0","id":2,"method":"minecraft:allowlist","params":[]},{"jsonrpc":"2.0","id":3,"method":"minecraft:allowlist/clear","params":[]}]
<- [{"jsonrpc":"2.0","id":1,"result":[{"id":"853c80ef-3c37-49fd-aa49-938b674adae6","name":"jeb_"}]},{"jsonrpc":"2.0","id":2,"result":[{"id":"853c80ef-3c37-49fd-aa49-938b674adae6","name":"jeb_"}]},{"jsonrpc":"2.0","id":3,"result":[]}]
<- {"jsonrpc":"2.0","method":"minecraft:notification/allowlist/added","params":[{"id":"853c80ef-3c37-49fd-aa49-938b674adae6","name":"jeb_"}]}
<- {"jsonrpc":"2.0","method":"minecraft:notification/allowlist/removed","params":[{"id":"853c80ef-3c37-49fd-aa49-938b674adae6","name":"jeb_"}]}
</syntaxhighlight>

# = 通用结构 =
在请求与通知中，有很多方法共通使用了同一种结构用于请求或响应。

# == PlayerDto ==
用于玩家相关的基础数据结构。

<div class="treeview"><section begin="player_dto"/>
*  JSON元素根标签
* * ：玩家的[UUID](UUID.md)。为带连字符的十六进制格式。
* * ：玩家名称。
<section end="player_dto"/>
|player_dto}}|no-remove=1}}
</div>

在请求时，UUID的读取优先级高于名称。如果两项数据均不设置，游戏将忽略此无效数据，不抛出错误。

# == UserBanDto ==
用于封禁玩家列表的基础结构。

<div class="treeview"><section begin="user_ban_dto"/>
*  JSON元素根标签
* * ：封禁的过期时间。格式参见[封禁列表存储格式](封禁列表存储格式.md)。
* * ：一个玩家。
* **
* * ：封禁的原因。
* * ：封禁的操作者。
<section end="user_ban_dto"/>
|user_ban_dto}}|no-remove=1}}
</div>

# == IpBanDto ==
用于封禁IP列表的基础结构。

<div class="treeview"><section begin="ip_ban_dto"/>
*  JSON元素根标签
* * ：封禁的过期时间。格式参见[封禁列表存储格式](封禁列表存储格式.md)。
* * ：一个IP地址。
* * ：封禁的原因。
* * ：封禁的操作者。
<section end="ip_ban_dto"/>
|ip_ban_dto}}|no-remove=1}}
</div>

# == OperatorDto ==
用于管理员列表的基础结构。

<div class="treeview"><section begin="operator_dto"/>
*  JSON元素根标签
* * ：玩家是否可以不受最大玩家数量限制进入服务端。
* * ：玩家的[权限等级](权限等级.md)。
* * ：一个玩家。
* **
<section end="operator_dto"/>
|operator_dto}}|no-remove=1}}
</div>

# == ServerState ==
服务端状态的基本结构。

<div class="treeview"><section begin="server_state"/>
*  JSON元素根标签
* * ：服务端在线的玩家。
* **
* ***
* * ：服务端是否已启动。
* * ：服务端版本信息。
* ** ：版本名称。
* ** ：服务端的[协议版本](协议版本.md)。
<section end="server_state"/>
|server_state}}|no-remove=1}}
</div>

# == TypedRule ==
游戏规则信息的基本结构。

<div class="treeview"><section begin="typed_rule"/>
*  JSON元素根标签
* * ：[游戏规则](游戏规则.md)的ID。
* * ：游戏规则的值类型。只可能是<code>boolean</code>或<code>integer</code>。
* * ：游戏规则的值。类型与对应游戏规则有关。
<section end="typed_rule"/>
|typed_rule}}|no-remove=1}}
</div>

# = 请求方法 =
下文中，对于无参数的请求，请求对应的章节内仅包含响应JSON结构；有参数的请求则同时包含负载和响应两部分。

# == 白名单管理 ==
白名单相关方法共有5个，分别用于获取、设置、添加、修改及清空[白名单](白名单存储格式.md)。

# === allowlist ===
<div class="treeview">
*  <code>allowlist</code>
* * ：一项在白名单中的玩家。
* **
</div>

# === allowlist/set ===
; 负载
<div class="treeview">
*  <code>players</code>
* * ：一项在白名单中的玩家。
* **
</div>

; 响应
<div class="treeview">
*  <code>allowlist</code>
* * ：一项在白名单中的玩家。此数组与传入负载一致。
* **
</div>

# === allowlist/add ===
; 负载
<div class="treeview">
*  <code>add</code>
* * ：一项要加入白名单中的玩家。
* **
</div>

; 响应
<div class="treeview">
*  <code>allowlist</code>
* * ：一项在白名单中的玩家。
* **
</div>

# === allowlist/remove ===
; 负载
<div class="treeview">
*  <code>remove</code>
* * ：一项要被移除于白名单中的玩家。
* **
</div>

; 响应
<div class="treeview">
*  <code>allowlist</code>
* * ：一项在白名单中的玩家。
* **
</div>

# === allowlist/clear ===
<div class="treeview">
*  <code>allowlist</code>
* * ：一项白名单中的玩家。
* **
</div>

# == 封禁玩家列表管理 ==
封禁玩家列表相关方法共有5个，分别用于获取、设置、添加、修改及清空[封禁玩家列表](封禁列表存储格式.md)。

# === bans ===
<div class="treeview">
*  <code>banlist</code>
* * ：一项在封禁玩家列表中的玩家。
* **
</div>

# === bans/set ===
; 负载
<div class="treeview">
*  <code>bans</code>
* * ：一项在封禁玩家列表中的玩家。
* **
</div>

; 响应
<div class="treeview">
*  <code>banlist</code>
* * ：一项在封禁玩家列表中的玩家。此数组与传入负载一致。
* **
</div>

# === bans/add ===
; 负载
<div class="treeview">
*  <code>add</code>
* * ：一项要加入封禁玩家列表中的玩家。
* **
</div>

; 响应
<div class="treeview">
*  <code>banlist</code>
* * ：一项在封禁玩家列表中的玩家。
* **
</div>

# === bans/remove ===
; 负载
<div class="treeview">
*  <code>remove</code>
* * ：一项要被移除于封禁玩家列表中的玩家。
* **
</div>

; 响应
<div class="treeview">
*  <code>banlist</code>
* * ：一项在封禁玩家列表中的玩家。
* **
</div>

# === bans/clear ===
<div class="treeview">
*  <code>banlist</code>
* * ：一项在封禁玩家列表中的玩家。
* **
</div>

# == 封禁IP列表管理 ==
封禁IP列表相关方法共有5个，分别用于获取、设置、添加、修改及清空[封禁IP列表](封禁列表存储格式.md)。

# === ip_bans ===
<div class="treeview">
*  <code>banlist</code>
* * ：一项在封禁IP列表中的玩家。
* **
</div>

# === ip_bans/set ===
; 负载
<div class="treeview">
*  <code>banlist</code>
* * ：一项在封禁IP列表中的玩家。
* **
</div>

; 响应
<div class="treeview">
*  <code>banlist</code>
* * ：一项在封禁IP列表中的玩家。此数组与传入负载一致。
* **
</div>

# === ip_bans/add ===
; 负载
<div class="treeview">
*  <code>add</code>
* * ：一项要加入封禁IP玩家列表中的IP。
* ** ：封禁的过期时间。
* ** ：一个IP。
* ** ：一个玩家，游戏将获取此玩家的IP地址。
* ***
* ** ：封禁的原因。
* ** ：封禁的操作者。
</div>

; 响应
<div class="treeview">
*  <code>banlist</code>
* * ：一项在封禁IP列表中的IP。
* **
</div>

# === ip_bans/remove ===
; 负载
<div class="treeview">
*  <code>ip</code>
* * ：一个要被移除于封禁IP列表中的IP。
</div>

; 响应
<div class="treeview">
*  <code>banlist</code>
* * ：一项在封禁IP列表中的IP。
* **
</div>

# === ip_bans/clear ===
<div class="treeview">
*  <code>banlist</code>
* * ：一项在封禁IP列表中的IP。
* **
</div>

# == 玩家管理 ==
玩家相关方法共有2个，分别用于获取玩家列表和踢出玩家。

# === players ===
<div class="treeview">
*  <code>players</code>
* * ：一个在线的玩家。
* **
</div>

# === players/kick ===
; 负载
<div class="treeview">
*  <code>kick</code>
* * ：要踢出的玩家。
* ** ：踢出玩家的原因。
* ** ：要踢出的玩家列表。
* ***
* ****
</div>

; 响应
<div class="treeview">
*  <code>players</code>
* * ：一项玩家。此数组仅包含实际被踢出的玩家。
* **
</div>

# == 管理员列表管理 ==
管理员列表相关方法共有5个，分别用于获取、设置、添加、修改及清空[管理员列表](管理员列表存储格式.md)。

# === operators ===
<div class="treeview">
*  <code>operators</code>
* * ：一项在管理员列表中的玩家。
* **
</div>

# === operators/set ===
; 负载
<div class="treeview">
*  <code>operators</code>
* * ：一项在管理员列表中的玩家。
* **
</div>

; 响应
<div class="treeview">
*  <code>operators</code>
* * ：一项在管理员列表中的玩家。此数组与传入负载一致。
* **
</div>

# === operators/add ===
; 负载
<div class="treeview">
*  <code>add</code>
* * ：一项要加入管理员列表中的玩家。
* **
</div>

; 响应
<div class="treeview">
*  <code>operators</code>
* * ：一项在管理员列表中的玩家。
* **
</div>

# === operators/remove ===
; 负载
<div class="treeview">
*  <code>remove</code>
* * ：一项要被移除于管理员列表中的玩家。
* **
</div>

; 响应
<div class="treeview">
*  <code>operators</code>
* * ：一项在管理员列表中的玩家。
* **
</div>

# === operators/clear ===
<div class="treeview">
*  <code>operators</code>
* * ：一项在管理员列表中的玩家。
* **
</div>

# == 服务端状态管理 ==
服务端状态相关方法共有4个。

# === server/status ===
<div class="treeview">
*  <code>status</code>
* *
</div>

# === server/save ===
; 负载
<div class="treeview">
*  <code>flush</code>：是否立刻保存服务端数据。
</div>

; 响应
<div class="treeview">
*  <code>save</code>：是否保存成功。
</div>

# === server/stop ===
<div class="treeview">
*  <code>stopping</code>：是否已停止服务端。
</div>

# === server/system_message ===
; 负载
<div class="treeview">
*  <code>message</code>
* * ：要发送的消息。如果不存在任何有效文本则响应直接为<code>false</code>。
* ** ：（存在时无效）要发送的文本。
* ** ：一个[本地化键名](本地化键名.md)，游戏会根据当前语言显示文本。
* ** ：替换本地化文本内的参数。处理方法详见。
* *** ：一个参数。
* * ：此消息是否在[动作栏](动作栏.md)而不是在聊天栏显示。
* * ：若指定，则仅这些玩家可接收此消息。
* **
* ***
</div>

; 响应
<div class="treeview">
*  <code>sent</code>：是否有玩家接收到了此服务端消息。
</div>

# == 服务端设置管理 ==
服务端设置相关方法共有40个。部分设置亦在[服务端配置文件](服务端配置文件.md)中。

下列方法的ID均遵守<code>serversettings/*</code>和<code>serversettings/*/set</code>的格式，它们的负载（若有）和响应的格式也类似：

# === serversettings/autosave ===
<div class="treeview">
*  <code>enabled</code>：查询结果。
</div>

# === serversettings/autosave/set ===
; 负载
<div class="treeview">
*  <code>enable</code>：将设置的值。
</div>

; 响应
<div class="treeview">
*  <code>enabled</code>：设置的值。此值与传入负载一致。
</div>

# === serversettings/difficulty ===
<div class="treeview">
*  <code>difficulty</code>：查询结果。
</div>

# === serversettings/difficulty/set ===
; 负载
<div class="treeview">
*  <code>difficulty</code>：将设置的值。取值只能为。
</div>

; 响应
<div class="treeview">
*  <code>difficulty</code>：设置的值。此值与传入负载一致。
</div>

# === serversettings/enforced ===
<div class="treeview">
*  <code>enforced</code>：查询结果。
</div>

# === serversettings/enforced/set ===
; 负载
<div class="treeview">
*  <code>enforce</code>：将设置的值。
</div>

; 响应
<div class="treeview">
*  <code>enforced</code>：设置的值。此值与传入负载一致。
</div>

# === serversettings/use_allowlist ===
<div class="treeview">
*  <code>used</code>：查询结果。
</div>

# === serversettings/use_allowlist/set ===
; 负载
<div class="treeview">
*  <code>use</code>：将设置的值。
</div>

; 响应
<div class="treeview">
*  <code>used</code>：设置的值。此值与传入负载一致。
</div>

# === serversettings/max_players ===
<div class="treeview">
*  <code>max</code>：查询结果。
</div>

# === serversettings/max_players/set ===
; 负载
<div class="treeview">
*  <code>max</code>：将设置的值。
</div>

; 响应
<div class="treeview">
*  <code>max</code>：设置的值。此值与传入负载一致。
</div>

# === serversettings/pause_when_empty_seconds ===
<div class="treeview">
*  <code>seconds</code>：查询结果。
</div>

# === serversettings/pause_when_empty_seconds/set ===
; 负载
<div class="treeview">
*  <code>seconds</code>：将设置的值。
</div>

; 响应
<div class="treeview">
*  <code>seconds</code>：设置的值。此值与传入负载一致。
</div>

# === serversettings/player_idle_timeout ===
<div class="treeview">
*  <code>seconds</code>：查询结果。数据类型和值取决于具体的方法。
</div>

# === serversettings/player_idle_timeout/set ===
; 负载
<div class="treeview">
*  <code>seconds</code>：将设置的值。数据类型和值取决于具体的方法。
</div>

; 响应
<div class="treeview">
*  <code>seconds</code>：设置的值。此值与传入负载一致。
</div>

# === serversettings/allow_flight ===
<div class="treeview">
*  <code>allowed</code>：查询结果。
</div>

# === serversettings/allow_flight/set ===
; 负载
<div class="treeview">
*  <code>allow</code>：将设置的值。
</div>

; 响应
<div class="treeview">
*  <code>allowed</code>：设置的值。此值与传入负载一致。
</div>

# === serversettings/motd ===
<div class="treeview">
*  <code>message</code>：查询结果。
</div>

# === serversettings/motd/set ===
; 负载
<div class="treeview">
*  <code>message</code>：将设置的值。
</div>

; 响应
<div class="treeview">
*  <code>message</code>：设置的值。此值与传入负载一致。
</div>

# === serversettings/spawn_protection_radius ===
<div class="treeview">
*  <code>radius</code>：查询结果。
</div>

# === serversettings/spawn_protection_radius/set ===
; 负载
<div class="treeview">
*  <code>radius</code>：将设置的值。
</div>

; 响应
<div class="treeview">
*  <code>radius</code>：设置的值。此值与传入负载一致。
</div>

# === serversettings/force_game_mode ===
<div class="treeview">
*  <code>forced</code>：查询结果。
</div>

# === serversettings/force_game_mode/set ===
; 负载
<div class="treeview">
*  <code>force</code>：将设置的值。
</div>

; 响应
<div class="treeview">
*  <code>forced</code>：设置的值。此值与传入负载一致。
</div>

# === serversettings/game_mode ===
<div class="treeview">
*  <code>mode</code>：查询结果。
</div>

# === serversettings/game_mode/set ===
; 负载
<div class="treeview">
*  <code>mode</code>：将设置的值。取值只能为。
</div>

; 响应
<div class="treeview">
*  <code>mode</code>：设置的值。此值与传入负载一致。
</div>

# === serversettings/view_distance ===
<div class="treeview">
*  <code>distance</code>：查询结果。
</div>

# === serversettings/view_distance/set ===
; 负载
<div class="treeview">
*  <code>distance</code>：将设置的值。
</div>

; 响应
<div class="treeview">
*  <code>distance</code>：设置的值。此值与传入负载一致。
</div>

# === serversettings/simulation_distance ===
<div class="treeview">
*  <code>distance</code>：查询结果。
</div>

# === serversettings/simulation_distance/set ===
; 负载
<div class="treeview">
*  <code>distance</code>：将设置的值。
</div>

; 响应
<div class="treeview">
*  <code>distance</code>：设置的值。此值与传入负载一致。
</div>

# === serversettings/accept_transfers ===
<div class="treeview">
*  <code>accepted</code>：查询结果。
</div>

# === serversettings/accept_transfers/set ===
; 负载
<div class="treeview">
*  <code>accept</code>：将设置的值。
</div>

; 响应
<div class="treeview">
*  <code>accepted</code>：设置的值。此值与传入负载一致。
</div>

# === serversettings/status_heartbeat_interval ===
<div class="treeview">
*  <code>seconds</code>：查询结果。
</div>

# === serversettings/status_heartbeat_interval/set ===
; 负载
<div class="treeview">
*  <code>seconds</code>：将设置的值。
</div>

; 响应
<div class="treeview">
*  <code>seconds</code>：设置的值。此值与传入负载一致。
</div>

# === serversettings/operator_user_permission_level ===
<div class="treeview">
*  <code>level</code>：查询结果。
</div>

# === serversettings/operator_user_permission_level/set ===
; 负载
<div class="treeview">
*  <code>level</code>：将设置的值。
</div>

; 响应
<div class="treeview">
*  <code>level</code>：设置的值。此值与传入负载一致。
</div>

# === serversettings/hide_online_players ===
<div class="treeview">
*  <code>hidden</code>：查询结果。
</div>

# === serversettings/hide_online_players/set ===
; 负载
<div class="treeview">
*  <code>hide</code>：将设置的值。
</div>

; 响应
<div class="treeview">
*  <code>hidden</code>：设置的值。此值与传入负载一致。
</div>

# === serversettings/status_replies ===
<div class="treeview">
*  <code>enabled</code>：查询结果。
</div>

# === serversettings/status_replies/set ===
; 负载
<div class="treeview">
*  <code>enable</code>：将设置的值。
</div>

; 响应
<div class="treeview">
*  <code>enabled</code>：设置的值。此值与传入负载一致。
</div>

# === serversettings/entity_broadcast_range ===
<div class="treeview">
*  <code>percentage_points</code>：查询结果。
</div>

# === serversettings/entity_broadcast_range/set ===
; 负载
<div class="treeview">
*  <code>percentage_points</code>：将设置的值。
</div>

; 响应
<div class="treeview">
*  <code>percentage_points</code>：设置的值。此值与传入负载一致。
</div>

# == 游戏规则管理 ==
[游戏规则](游戏规则.md)相关方法共有2个。

# === gamerules ===
<div class="treeview">
*  <code>gamerules</code>
* * ：一项游戏规则。
* **
</div>

# === gamerules/update ===
; 负载
<div class="treeview">
*  <code>gamerule</code>
* * ：游戏规则的ID。
* * ：游戏规则的值。类型与对应游戏规则有关。
</div>

; 响应
<div class="treeview">
*  <code>gamerule</code>
* *
</div>

# = 通知方法 =
通知是服务端发向客户端的数据，不存在<code>id</code>。下文中没有负载的通知将不单独以章节介绍，有负载的通知对应章节仅包含负载结构。

# == 服务端状态 ==
有关服务端状态的通知均不包含负载。

# == 玩家 ==
有关玩家的通知共有2个。

# === players/joined ===
<div class="treeview">
*  <code>player</code>：加入服务端的玩家。
* *
</div>

# === players/left ===
<div class="treeview">
*  <code>player</code>：离开服务端的玩家。
* *
</div>

# == 管理员列表更改 ==
有关管理员列表更改的通知共有2个。

# === operators/added ===
<div class="treeview">
*  <code>player</code>：新增的管理员的信息。
* *
</div>

# === operators/removed ===
<div class="treeview">
*  <code>player</code>：被撤销的管理员的信息。
* *
</div>

# == 白名单更改 ==
有关白名单更改的通知共有2个。

# === allowlist/added ===
<div class="treeview">
*  <code>player</code>：加入白名单的玩家。
* *
</div>

# === allowlist/removed ===
<div class="treeview">
*  <code>player</code>：从白名单中移除的玩家。
* *
</div>

# == 封禁IP列表更改 ==
有关封禁IP列表更改的通知共有2个。

# === ip_bans/added ===
<div class="treeview">
*  <code>player</code>：被封禁的IP。
* *
</div>

# === ip_bans/removed ===
<div class="treeview">
*  <code>player</code>：被解除封禁的IP地址。
</div>

# == 封禁玩家列表更改 ==
有关封禁玩家列表更改的通知共有2个。

# === ip_bans/added ===
<div class="treeview">
*  <code>player</code>：被封禁的玩家。
* *
</div>

# === ip_bans/removed ===
<div class="treeview">
*  <code>player</code>：被解除封禁的玩家。
* *
</div>

# == 游戏规则更改 ==
有关游戏规则更改的通知共有1个。

# === gamerules/updated ===
<div class="treeview">
*  <code>gamerule</code>：游戏规则信息。
* *
</div>

# == 服务端心跳 ==
有关服务端心跳的通知共有1个。

# === server/status ===
<div class="treeview">
*  <code>status</code>
* *
</div>

# 历史

|
|请求格式。<ref></ref>}}
|而不是此前的命名空间。}}
|
|。
|添加通知<code>server/activity</code>。}}
|
|。
|修改了游戏规则管理相关的请求和响应格式，以适配游戏规则注册表。}}
}}

# 参考

# 导航

[de:Minecraft-Server-Verwaltungsprotokoll](de:Minecraft-Server-Verwaltungsprotokoll.md)
[en:Minecraft Server Management Protocol](en:Minecraft Server Management Protocol.md)
[pt:Protocolo de Gerenciamento de Servidor do Minecraft](pt:Protocolo de Gerenciamento de Servidor do Minecraft.md)