# 战利品表/数值提供器

*页面ID: 100140* | *来源: Minecraft Wiki*

<div class="treeview"><onlyinclude>
* * ：（可能不存在，默认为<code>uniform</code>）数值提供器的类型。
* *
* * ：进行独立重复的[伯努利试验](wzh:伯努利试验.md)的次数。
* **
* * ：每次试验的成功概率。
* **
* *
* * ：一个任意的确切数值。
* *
* * ：使用魔咒等级计算数值。
* **
* *
* * ：记分项数据源。如果直接指定目标，则需要战利品上下文提供的**实体**进行检测，值必须为<code>this</code>（自身）、<code>attacker</code>（执行攻击的实体）、<code>direct_attacker</code>（执行攻击的直接实体）和<code>attacking_player</code>（执行击杀的玩家）之一。如果不是直接指定目标，则标签内要附加下面的参数：
* ** ：数据源类型。需为<code>context</code>（当前上下文中的某项数据源）或<code>fixed</code>（固定来源）。
* **
* ** ：记分项数据源，需要战利品上下文提供的**实体**进行检测。必须为之一。
* **
* ** ：一个UUID或玩家名。
* * ：需要查询的记分项。
* * ：（可选，默认为1.0）记分项的值将乘以该倍率后返回。
* *
* * ：（命名空间ID）表示一个命令存储。
* * ：NBT路径，表示一个NBT标签。
* *
* * ：最小值。
* **
* * ：最大值。
* **
* * }}
* * ：要作和的数值提供器。
* ** ：一个数值提供器。
* ***
</onlyinclude></div><noinclude>
[Category:顶级数据值页面](Category:顶级数据值页面.md)
[Category:通过Nbt inherit加载的页面](Category:通过Nbt inherit加载的页面.md)
</noinclude>