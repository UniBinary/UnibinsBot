# Java版25w20a

*页面ID: 151457* | *来源: Minecraft Wiki*

<onlyinclude>

* *25w20a**是[Java版1.21.6](Java版1.21.6.md)的第6个快照，发布于2025年5月13日<ref></ref>，加入了[鞍](鞍.md)的[合成配方](合成配方.md)、新的[音乐](音乐.md)曲目和新的[音乐唱片](音乐唱片.md)，更新了[全景图](全景图.md)，并修复了一些漏洞。

# 新内容
# = 物品 =
* *[音乐唱片](音乐唱片.md)**
* 加入了新的音乐唱片Tears，作者是[Amos Roddy](Amos Roddy.md)。
* * 当[恶魂](恶魂.md)被玩家反弹的[火球](火球.md)杀死后掉落。
* * [稀有度](稀有度.md)为“少见”。
* * 通过[红石比较器](红石比较器.md)输出的红石信号强度为10。

* *[鞍](鞍.md)**
* 加入了合成配方：

  * 此配方在物品栏中获得首个[皮革](皮革.md)时解锁。

# = 游戏内容 =
* *[音乐](音乐.md)**
* 在樱花树林、森林、沙漠、雪林、繁茂洞穴生物群系和主菜单中加入了5首新的音乐曲目，作者是Amos Roddy：
* * Below and Above
* * Broken Clocks
* * Fireflies
* * Lilypad
* * O's Piano

* *[选项](选项.md)**
* 在“”界面中加入了“”选项，控制音乐在世界中播放的间歇时间，默认为20分钟。
* 在“”界面中加入了“”选项，控制音乐开始播放时及游戏暂停菜单中是否显示当前播放的音乐弹窗。

# = 命令格式 =
* ***
* 用于向客户端显示对话框的新命令。
* 语法：
* * <code>dialog show <targets> <dialog></code>
* ** 向玩家显示对话框。
* ** 返回<code>targets</code>参数中玩家的数量。
* * <code>dialog clear <targets></code>
* ** 清除玩家可见的对话框。
* ** 返回<code>targets</code>参数中玩家的数量。
* 参数：
* * <code><targets></code>：玩家名称、[UUID](UUID.md)或玩家选择器。
* * <code><dialog></code>：<code>dialog</code>注册表中的命名空间ID或内联对话框定义。

# = 常规 =
* *[数据包](数据包.md)**
* 加入了子文件夹。

* *[数据组件](数据组件.md)**
* 在物品堆叠组件<code>equippable</code>中：
* * 加入了可选字段，默认为<code>false</code>，True时允许玩家通过持[剪刀](剪刀.md)右键目标实体以移除此物品。
* * 加入了可选字段，需为一个[声音事件](Java版声音事件.md)，默认为<code>item.shears.snip</code>，控制此物品被剪下时所播放的音效。

* *[注册表](注册表.md)**
* 加入了固有注册表。
* 加入了可写注册表<code>DIALOG</code>。

* *[对话框](对话框.md)**
* 为玩家展示简单对话框的新方法。
* 对话框通常存储在<code>dialog</code>注册表中，但也可以内联定义。
* 目前是实验性功能。
* 为避免混淆对话框和游戏UI，自定义对话框标题部分会有一个警示标志。点击此警示标志后：
* * 提示玩家此对话框为第三方提供。
* * 提供强制退出世界的选项，避免玩家因进入无退出选项的对话框而陷入死循环。
* 服务端也可以在<code>play</code>和<code>configuration</code>阶段展示对话框，但是<code>configuration</code>阶段只接受内联定义。
* 对话框会在玩家作出选择后关闭。
* 默认情况下按下也会尝试退出对话框。如果对话框设定了特殊的退出行为则也会执行此行为，是否可通过按下退出对话框也可以配置。
* 对话框屏幕关闭后，玩家将进入上一个对话框或返回世界。
* 单人模式下打开对话框会暂停游戏。
* 对话框可以配置为在暂停菜单中进入，这拓展了之前添加的按钮。
* 大多数对话框都遵循下列通用布局：
* * 页首，包括标题和警示标志
* * 正文内容，取决于对话框类型
* * 页脚，包括主按钮和提交按钮
* 对话框可以在数据包的目录下定义。
* 由于对话框的各种形式会复用一部分标签，因此先行列出。
* * 通用标签：
* ** ：对话框类型。
* ** ：文本组件，对话框标题。
* ** ：可选，文本组件，用于覆写展示此对话框的标题。
* ** ：对话框主体，默认为空列表。
* ** ：是否可以通过按下来退出对话框。
* * 按钮通用标签：
* ** ：文本组件，按钮的文本。
* ** ：可选，文本组件，按钮高亮的提示文本。
* ** ：按钮的宽度，默认为150。
* 点击动作
* * 对话框中的这些元素被点击后会触发指定的事件。字段如下：
* ** *按钮通用标签*
* ** ：点击后的点击事件。不指定时为关闭此对话框。
* 对话框类型
* *
* ** 一个页脚包含一个按钮的屏幕。
* ** 退出动作由指定。
* ** 字段如下：
* *** *通用标签*
* *** ：点击行为，默认为。
* *
* ** 页脚有两个按钮的屏幕。
* ** 退出行为由指定。
* ** 字段如下：
* *** *通用标签*
* *** ：点击行为。
* *** ：点击行为。
* *
* ** 一个可以滚动的列表。
* ** 页脚依旧提供退出的按钮。
* *** 未指定时，按钮文本为，点击行为为。
* ** 退出动作由指定。
* ** 字段如下：
* *** *通用标签*
* *** ：点击动作的非空列表。
* *** ：点击事件。
* *** ：正整数，按钮的列数。默认为2。
* *
* ** 一个可以滚动的列表，来源网络数据包。
* ** 页脚依旧提供退出的按钮。
* *** 行为与一致。
* ** 退出行为由指定。
* ** 字段如下：
* *** *通用标签*
* *** ：点击事件。
* *** ：正整数，按钮的列数。默认为2。
* *** ：正整数，按钮的宽度，默认为150。
* *
* ** 一个可以指向其他对话框的可滚动列表。
* *** 这些对话框使用作为预览标题。
* ** 页脚依旧提供退出的按钮。
* *** 行为与一致。
* ** 退出行为由指定。
* ** 字段如下：
* *** *通用标签*
* *** ：一个对话框，或对话框的列表（不可混用命名空间ID和内联定义形式），或一个对话框标签。
* *** ：点击事件。
* *** ：正整数，按钮的列数。默认为2。
* *** ：正整数，按钮的宽度，默认为150。
* *
* ** 允许玩家输入数据的屏幕。
* ** 页脚有一个提交按钮。
* ** 退出行为：无（屏幕关闭后会将输入数据发送到服务端）
* ** 字段如下：
* *** *通用标签*
* *** ：非空的输入控制列表。
* *** ：提交按钮。
* *
* ** 允许玩家输入数据的屏幕。
* ** 允许有多个提交按钮。
* ** 没有页脚部分。
* ** 退出动作：无（屏幕关闭后会将输入数据发送到服务端）
* ** 字段如下：
* *** *通用标签*
* *** ：非空的输入控制列表。
* *** ：非空的提交按钮列表。
* 对话框主体类型
* * 所有的对话框都可以定义对话框主体，对话框主体在页首之下。
* * 共通字段如下：
* ** ：对话框主体类型。
* *
* ** 一个多行的信息。
* ** 字段如下：
* *** ：文本组件。
* *** ：信息的最大宽度，默认为200。
* *
* ** 一个带描述的物品。
* *** 指定了后就如同物品栏中的物品提示信息一样。
* ** 物品会向在物品栏中一样渲染。
* ** 物品本身不受和影响。
* ** 字段如下：
* *** ：带数量的物品堆叠。
* *** ：可以为文本组件，也可以为：
* **** ：文本组件。
* **** ：内容的最大宽度，正整数，默认为200。
* *** ：如果为,则额外渲染物品数量、耐久度信息。默认为。
* *** ：如果为，则会渲染物品提示框。默认为。
* *** ：内容元素的宽度，默认为16。
* *** ：内容元素的高度，默认为16。
* 输入控制类型
* * 输入控制允许玩家输入不同的数据。
* * 当提交数据时，这些值会转换为字符串提交到服务端。
* * 通用字段如下：
* ** ：输入控制类型。
* ** ：一个标识符，只能为字母、数字或下划线。
* * .
* ** 文本输入。
* ** 数据不经修改即发送。
* ** 字段如下：
* *** ：输入宽度，默认为。
* *** ：文本组件，显示输入标题。
* *** ：输入是否可见，默认为 。
* *** ：初始值，默认为空。
* *
* ** 一个复选框，用以输入是或否。
* ** 字段如下：
* *** ：文本组件，渲染在复选框右侧。
* *** ：初始值。
* *** ：勾选时的值。默认为。
* *** ：不勾选时的值。默认为。
* *
* ** 可以通过点击切换选项的输入框。
* ** 发送当前选定的值。
* ** 字段如下：
* *** ：文本组件，按钮上的文本。
* *** ：文本是否在按钮内。默认为。
* *** ：按钮的宽度，默认为。
* *** ：可选值，格式如下：
* **** ：要发送的值。
* **** ：覆写选项展示的值（不影响发送的值）。
* **** ：是否是初始值。此列表只能有一个初始值。
* **** 也可以直接指定字符串，此时与只指定等同。
* *
* ** 一个可选滑块的输入。
* ** 发送当前值，但整数不会带小数点。
* ** 字段如下：
* *** ：文本组件，按钮的基础描述文本。
* *** ：按钮的文本格式。第一个参数为，第二个为当前值。
* *** ：按钮的宽度，默认为。
* *** ：左侧的值。
* *** ：右侧的值。
* *** ：正整数，决定此区间的有效值数。设置成1依旧有左右两侧的两个值。
* *** ：初始值，必须在区间里，默认为左侧的值。
* 提交行为
* * 每个输入对话框都有一个提交按钮。
* * 当点击按钮时，将收集所有输入的值，并使用下面描述的方法之一将其发送到服务端。
* * 字段提交的值包含。
* * 提交行为字段：
* ** *按钮通用标签*
* ** ：此提交行为的标识符。
* ** ：提交的动作。
* *** ：提交方式类型。
* ** 注意：服务端不会在命令或负载到达前验证单个值。
* 提交方法类型
* *
* ** 此方法会构建一个宏命令，并请求服务端运行。
* ** 与点击行为一样，若命令的权限等级大于0，游戏会弹出确认屏幕。
* *** 未被宏匹配的值会被忽略，无传入的宏参数会视为空。
* ** 字段如下：
* *** ：一个字符串，代表对应的宏。
* *
* ** 此方法会使用宏构建一个自定义的点击动作发送到服务端。
* *** 未被宏匹配的值会被忽略，无传入的宏参数会视为空。
* ** 字段如下：
* *** ：一个字符串，作为宏的负载。
* *** ：命名空间ID。
* *
* ** 此方法会构建一个服务端的自定义点击动作，发送所有值，并请求服务端执行。
* ** 格式：
* *** 键与值使用分隔。
* *** 键值间的换行使用。
* *** 如果和存在于任何键或值中，会被转化为和。
* ** 字段如下：
* *** ：命名空间ID。

* *[文本组件](文本组件.md)**
* 加入了点击事件<code>custom</code>。
* * 当玩家点击带有此样式的文本时，客户端会向服务端发送<code>minecraft:custom_click_action</code>专用封包。
* * 预期用途是作为修改过的服务端的<code>run_command</code>的替代品，这样就无需担心添加自定义的命令。
* * 此封包在原版服务端上没有任何作用。
<div class="treeview">
  :** ：命名空间ID。
  :** ：可选的字符串负载。
</div>
* 加入了点击事件<code>show_dialog</code>。
* * 当玩家点击带有此样式的文本时，客户端会打开指定的对话框。
<div class="treeview">
  :** ：要打开的对话框。
</div>

* *[Realms](Realms.md)**
* 在Realms加载屏幕中加入了Realms徽标。
* 在连接到Realms时加入了地区信息。

* *[纹理](纹理.md)**
* 加入了UI精灵图。

* *[标签](Java版标签.md)**
* 加入了空对话框标签<code>#pause_screen_additions</code>。

* *[闪烁标语](闪烁标语.md)**
* 加入了以下闪烁标语：
* * Music by Aaron Cherof!
* * Music by Kumi Tanioka!
* * Music by Amos Roddy!
* * Flint and Steel!
* * Chicken Jockey!

# 更改
# = 方块 =
* *[失水恶魂](失水恶魂.md)**
* 现在不会被[水流](水流.md)破坏。

# = 物品 =
* *[苍白橡木船](苍白橡木船.md)和[苍白橡木运输船](苍白橡木运输船.md)**
* 更改了纹理。

# = 生物 =
* *[马](马.md)、[骡](骡.md)、[驴](驴.md)、[骆驼](骆驼.md)、[骷髅马](骷髅马.md)、[僵尸马](僵尸马.md)、[猪](猪.md)、[炽足兽](炽足兽.md)、[羊驼](羊驼.md)和[快乐恶魂](快乐恶魂.md)**
* 现在可以对其[剪刀](剪刀.md)以剪下其所佩戴的鞍、[马铠](马铠.md)、[挽具](挽具.md)或[地毯](地毯.md)。
* * 当这些实体正被骑乘，或玩家[潜行](潜行.md)使用剪刀时，上述行为不会发生。
* * 带有剪刀的[发射器](发射器.md)不能从上述生物上移除相应的佩戴物。

* *[恶魂](恶魂.md)**
* 现在默认具有0.06[飞行速度](属性/飞行速度.md)属性。

# = 非生物实体 =
* *[被激活的TNT](被激活的TNT.md)**
* 部分回退了对的修复；现在，TNT在水中爆炸时再次会破坏物品实体和盔甲架，但仍不会破坏画和物品展示框等装饰性实体。

# = 世界生成 =
* *[战利品表](战利品表.md)**
* 移除了以下结构战利品箱中的鞍，并以个[皮革](皮革.md)代替：
* * [地牢](地牢.md)战利品箱
* * [远古城市](远古城市.md)战利品箱
* * [沙漠神殿](沙漠神殿.md)战利品箱
* * [丛林神庙](丛林神庙.md)战利品箱
* * [要塞](要塞.md)祭坛战利品箱

# = 游戏内容 =
* *[属性](属性.md)**
* 现在[飞行速度](属性/飞行速度.md)属性（<code>flying_speed</code>）影响[恶魂](恶魂.md)和[快乐恶魂](快乐恶魂.md)的飞行速度。

* *[音乐](音乐.md)和[声音](声音.md)**
* 现在游戏暂停时，音乐也会播放。

* *[迷雾](迷雾.md)**
* 现在迷雾对3D[平视显示器](平视显示器.md)元素生效（如第一人称下玩家的手和手中的物品）。
* 降低了[降雨](降雨.md)和[雷暴](雷暴.md)天气下的迷雾浓度。
* 现在[天空光照](天空光照.md)降低到8（此前为0）时，雨雾浓度达到最低。

# = 常规 =
* *[全景图](全景图.md)**
* 更新了主菜单全景图以匹配[追逐天空](追逐天空.md)。

* *[数据包](数据包.md)**
* 将数据包版本号更改为。

* *[文本组件](文本组件.md)**
* 现在聊天屏幕和成书屏幕中点击事件<code>run_command</code>指定的命令若无法解析或所需的权限等级大于0，则玩家点击文本后会出现一个确认屏幕。

* *[资源包](资源包.md)**
* 将资源包版本号更改为。

* *[闪烁标语](闪烁标语.md)**
* 移除了“Minors welcome!”。
* 将“Pumpa kungen!”闪烁标语改为“Pumpakungen!”。
* 将“10 years of Mining and Crafting!”闪烁标语改为“15 years of Mining and Crafting!”。

* *[暂停屏幕](暂停屏幕.md)**
* 游戏现在会根据数据包和服务端的网络数据包判断是否展示自定义的对话框。
* 此对话框依旧使用展示标题。
* 此功能由对话框标签参与处理：
* * 如果未指定此标签或此标签为空，但服务端发送了网络数据包，则显示。
* * 如果标签有一个元素，则会替换“”按钮，显示此对话框按钮。
* * 如果有多个元素，则按钮指向对话框，此对话框会调用标签依次显示对话框。

# 修复
</onlyinclude>

# 参考

# 导航

[de:25w20a](de:25w20a.md)
[en:Java Edition 25w20a](en:Java Edition 25w20a.md)
[es:Java Edition 25w20a](es:Java Edition 25w20a.md)
[fr:Édition Java 25w20a](fr:Édition Java 25w20a.md)
[ja:Java Edition 25w20a](ja:Java Edition 25w20a.md)
[pt:Edição Java 25w20a](pt:Edição Java 25w20a.md)
[ru:25w20a (Java Edition)](ru:25w20a (Java Edition).md)
[uk:25w20a (Java Edition)](uk:25w20a (Java Edition).md)