# 命令方块

*页面ID: 8744* | *来源: Minecraft Wiki*

</ref>||Mojavatar Jens 200x296.png}}
* *命令方块（Command Block）**是一种用于储存和执行[命令](命令.md)的[方块](方块.md)，常用于多人服务器与自定义地图中。

# 生成
命令方块不会生成在世界中。

# 获取

命令方块无法在未开启[作弊](作弊.md)的[生存模式](生存模式.md)的世界中获取。使用[创造模式](创造模式.md)或启用[作弊](作弊.md)，命令方块可以通过”，或“”中有命令方块）|JE}}、[创造模式](创造模式.md)中使用或[命令](命令.md)等方式获得。

# 破坏
命令方块不能被挖掘破坏。

[权限等级](权限等级.md)不足或的玩家，即使处于创造模式也不能破坏命令方块。

命令方块被破坏后不会掉落。

# 用途

满足预设条件时，命令方块会执行其内含的命令。可帮助不能使用命令的玩家执行一个特定的命令（如允许所有人通过获得一件特定物品，而非让他们直接使用获得任何他们想要的东西）。

命令方块拥有**朝向（Facing）**，决定哪个连锁型命令方块会受到感应，以及“”模式的命令方块会受哪个命令方块控制。

要允许命令方块执行命令，必须启用[游戏规则](游戏规则.md)。

命令方块的权限等级为或，不能执行需要更高权限等级的命令。

命令方块执行以下命令时，输出只会在“上一个输出”文本框显示，而不会在聊天框显示：、、、、和。

玩家无法在生存模式和冒险模式下打开命令方块的交互界面并修改其内容。

只有处于[创造模式](创造模式.md)且拥有对应管理员权限的玩家才能够放置或破坏命令方块。

命令方块不能被点燃。命令方块的[爆炸抗性](爆炸抗性.md)与[基岩](基岩.md)相同且不能被[活塞](活塞.md)推动。

# = 设置 =

要输入或修改命令，对命令方块点击键以打开命令方块的界面。只有处于[创造模式](创造模式.md)且拥有相关权限的玩家才能打开命令方块的界面。在[单人游戏](单人游戏.md)中，玩家必须处于创造模式并激活命令功能才能编辑命令方块。在[多人游戏](多人游戏.md)中，只能由创造模式下的[管理员](管理员.md)编辑。

在[单人游戏](单人游戏.md)中打开命令方块界面时会暂停游戏。

; 控制台命令
   命令可被输入到第一个文本框按可以补全命令且循环代入可选项。
   在命令方块中的命令：
  * 长度较长时，文本框一次只能显示其中的一小段。
  * 执行时的最大有效长度为32500个字符。
  * 最多只能输入32500个字符。
  * 超出32767个字符时可以正常保存，但当命令方块所在区块被重新加载后，会被截断至个字符。
   命令方块内的命令**可以**像在聊天框中一样加斜杠前缀（<code>/</code>），但也可以省略。
   在命令文本框下是一些如何使用[目标选择器](目标选择器.md)的小提示。

; 上一个输出
  下方的文本框显示上一个命令的输出（执行成功或失败）。起初它是或。这里的文本无法编辑。
  文本框或的按钮控制上一个输出是否储存并显示。它设置为输出时将显示为或（默认），设置为隐藏时将显示为或。当世界中有大量命令方块正在运行时，不显示输出可减少内存使用以优化卡顿。

[缩略图|**上方：**“”模式的命令方块<br>**下方：**“”模式的命令方块](File:Command block textures.png.md)
; 方块类型
   点击//”按钮}}或可改变命令方块的类型。默认为命令方块的种类。命令方块改变类型时也会改变颜色：
  * 脉冲型命令方块是橙色的。这种方块是“标准”的命令方块，激活一次执行一次命令。
  * 连锁型命令方块是青色的。这种方块只在指向它的方块执行命令时才会执行命令。指向它的命令方块可为任意类型的命令方块。
  * 循环型命令方块是紫色的。这种方块被激活时，或执行一次命令。

; 条件
   点击/”按钮}}或可改变命令方块的条件制约行为：
  * “”：只有当背后的命令方块成功执行命令时才会执行命令（“背后”的意思是该命令方块上箭头指向的反方向，无视连锁方向）。除了连锁型命令方块之外，脉冲型和循环型命令方块都需要至少1游戏刻（取决于“已选项中的延迟”）来激活。条件制约检测的是上一个命令方块在至少1游戏刻前是否成功而忽视同一游戏刻已经发生变化的状态，这就使得检测上一个命令方块是否成功会先于有条件的命令方块激活，即实际不在同一游戏刻内。不过延迟并不代表命令链在进程处理上的混乱，在较短的时间内命令链的结果仍然是相对有序的。
  * “”（默认）：无论背后的命令方块是否成功执行命令，该命令方块都会正常执行命令。

; 红石
  点击/”按钮}}或可改变命令方块的开启需求条件：
  * “”（脉冲型命令方块和循环型命令方块的默认设置）：命令方块必须接收到红石信号才能执行命令。
  * “”（连锁型命令方块的默认设置）：命令方块在没有红石信号时也会保持为开启状态。特别地，脉冲型命令方块在设置完毕后将立即激活，此后使用，或复制或放置带有NBT的命令方块才可以再次激活。

; |於第一個刻度執行}}
  指定循环型命令方块是否在[激活](#激活.md)后立即执行其命令。若被禁用，则从激活时起延迟后第一次执行。该选项对延迟为0刻的循环型命令方块没有影响。

; |以刻度計算延遲}}
  对于脉冲型命令方块和连锁型命令方块，指定在被激活或触发后，执行命令之前延迟的游戏刻。对于循环型命令方块，指定其重新执行命令所延迟的游戏刻。对于脉冲型命令方块和循环型命令方块，延迟设置为0和1的效果相同，游戏将把0视为1。但对于连锁型命令方块，0和1不同。当被设置延迟的命令方块被破坏，未执行的命令将不会执行。

;
  指定命令方块的名称，在指向该方块时显示。它也用于[消息命令](#消息.md)。若命令方块有在铁砧中命名，放置时将采用此名称。若为空，则默认为<code>!</code>。

; 完成与取消
  }}或}}会保存命令并退出命令方块界面。
  点击“取消”按钮或}}会在不保存任何修改的情况下退出命令方块界面。

# = 激活 =
处于“”模式的命令方块是[机械元件](红石元件#机械元件.md)，可被下列形式激活：
* 毗邻的开启的**[电源](红石元件#电源.md)**（例外：[红石火把](红石火把.md)不会激活其所附着的命令方块，[侦测器](侦测器.md)只会激活指向的命令方块）。
* 毗邻的**[充能方块](红石电路/充能与激活.md)**。
* 毗邻的指向命令方块且激活的[红石中继器](红石中继器.md)、[红石比较器](红石比较器.md)或[红石粉](红石粉.md)（包括命令方块上方的红石粉）；毗邻但未指向命令方块的红石粉不会激活命令方块。

在同一红石刻内，电容器更新阶段前存在，但在电容器更新阶段被取消的信号无法激活命令方块。

命令方块也是[红石导体](红石导体.md)，因此可被充能。
<!--
疑似过时：另外，当命令方块由命令复制到一个会被激活的位置时，新的命令方块只有在原有命令未被执行过时才会执行命令。（可从F3调试屏幕的“triggered”看到）。--><!--
过时内容：不像除了[音符盒](音符盒.md)的其他红石元件，命令方块可在每个[游戏刻](游戏刻.md)激活一次（每秒20次，在不卡顿的情况下）。然而，在两个游戏刻中都激活一次需要使其在同一刻钟从被激活转为未激活状态（见[fill+replace高频](Tutorial:高频电路#/fill+replace高频.md)的实例）。-->

处于“”的命令方块将一直被激活。

命令方块有1游戏刻的延迟，当它接收[上升沿](脉冲电路#上升沿.md)时将在1游戏刻后做出反应（激活或撤销激活）。

当被激活时，命令方块将尝试执行它的命令。激活后的动作将取决于命令方块的类型，见[上文](#方块类型.md)。

# == 连锁执行 ==
若一个指向连锁型命令方块的命令方块被激活，它将向连锁型命令方块发出准备执行命令的信号，而无论其是否执行命令。连锁型命令方块只有在被激活开启时（在“条件制约”模式下还要在它所指向方向的反方向的命令方块成功执行命令时）才会执行命令。

连锁中的方块将在同一游戏刻执行命令。连锁执行的信号无法传递给一个已经在这个游戏刻执行过命令的方块。游戏规则决定了连锁型命令方块能连锁执行的总数量，默认为65536。

# = 输出 =
当激活时，命令方块可以有几种不同类型的输出：

# == 成功次数 ==
红石比较器可检测并输出命令方块的成功次数。成功次数是0到2,147,483,647（含）之间的整数。信号强度总是反映命令方块最后一次执行的结果，即便命令方块取消激活。

成功次数通常是0或1，除了命令。成功次数通常与命令有关（例如，被命令所影响的玩家数，改变的方块数，命令的返回值）。

点击“完成”按钮或按下退出界面时，无论是否作出修改均会将成功次数置零。在有更改的情况下退出界面会将成功次数置零。

# == 消息 ==
输出消息描述执行命令的成功与否，会有几种输出位置：

* 上一个输出：输出消息都会写入命令方块GUI的“上一个输出”文本框里。
* 聊天框：输出消息会写入到聊天框，在多人模式下只对所有管理员显示。将游戏规则设为<code>false</code>可关闭在聊天框的输出。一些命令可能会在聊天框写入其正常工作时的附带文本，这不会被拦截。（比如，命令将在所有玩家的聊天框写入消息）。带有执行者的名字作为前缀的聊天信息（比如命令，和）会使用<code>@</code>/<code>!</code>作为执行者名字。如果命令方块已经被命名过，那么输出时就会使用命名的名字。
* 日志：输出信息会写入到多人游戏服务器日志被设为<code>false</code>}}。

# = 彩蛋 =

在命令方块中不加“<code>/</code>”运行命令“[Searge](Searge.md)”会把上一个输出设为“#itzlipofutzli”。

# = 安全保护 =

命令方块属于“仅管理员可设置数据”的方块。

非管理员玩家对命令方块按下+时不会复制方块实体数据，放置物品栏中已有的带有方块实体数据的这些方块也不会将数据实际加载。

管理员玩家的物品栏中带有方块实体数据的命令方块的物品[提示框](提示框.md)中会显示警告信息。

# 音效

# 数据值
# = ID =
：

：

# = 方块数据 =

命令方块的方块数据用于确定方块朝向与条件制约模式：

# = 方块状态 =

# = 方块实体 =

命令方块有包含方块附加数据的方块实体。

：

：

# 视频

# 历史

||[Jeb](Jeb.md)开始发一些关于扩展生存模式的新方块的推文。他开始推送一些图片，想法和方块将来用法的线索。}}
|
|
|
|。}}
|
|
|
|
|
|</ref>}}
|
|
|
|这类[命令](命令.md)的玩家不再能放置命令方块。 }}
|
|将随机使用各种不同的幽默短语作为上一个输出。}}
|
|
|
|
|
|
|补全命令了。}}
|
|
|
|
|
|
|
|
|而不是。}}
|
|
|
|
|
}}

# 画廊
<gallery>
File:JebAdventureModeControlBlockDev1.png|Mojang[ 发布]的第一张命令方块的截图
File:JebAdventureModeControlBlockDev2.png|Mojang[ 发布]的第二张截图，展示着命令方块正在给予Jeb玻璃
</gallery>

# 参考

# 导航

[Category:红石电路](Category:红石电路.md)

[cs:Příkazový blok](cs:Příkazový blok.md)
[de:Befehlsblock](de:Befehlsblock.md)
[en:Command Block](en:Command Block.md)
[es:Bloque de comandos](es:Bloque de comandos.md)
[fr:Bloc de commande](fr:Bloc de commande.md)
[it:Blocco comandi](it:Blocco comandi.md)
[ja:コマンドブロック](ja:コマンドブロック.md)
[ko:명령 블록](ko:명령 블록.md)
[nl:Opdrachtblok](nl:Opdrachtblok.md)
[pl:Blok poleceń](pl:Blok poleceń.md)
[pt:Bloco de Comando](pt:Bloco de Comando.md)
[ru:Командный блок](ru:Командный блок.md)
[th:บล็อกคำสั่ง](th:บล็อกคำสั่ง.md)
[uk:Командний блок](uk:Командний блок.md)