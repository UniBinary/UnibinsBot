# Java版1.21.5

*页面ID: 138814* | *来源: Minecraft Wiki*

* *1.21.5**是**[春意盎然](春意盎然.md)**的正式版，也是的一次小更新，发布于2025年3月25日。<ref></ref>此次更新加入了[野花簇](野花簇.md)、[枯叶堆](枯叶堆.md)、[灌木丛](灌木丛.md)、[枯草丛](枯草丛.md)、[仙人掌花](仙人掌花.md)等大量的新方块和少量新物品，丰富了[猪](猪.md)、[牛](牛.md)、[鸡](鸡.md)、[绵羊](绵羊.md)和[狼](狼.md)的变种生成机制，正式加入了[实验性内容](实验性内容.md)“”中对[流浪商人](流浪商人.md)和[制图师](制图师.md)的更改以及[游戏测试系统](测试方块.md)，同步了中的[倒下的树木](倒下的树木.md)与游戏规则<code>tntExplodes</code>，并修复了一些漏洞。此版本不兼容1.21到1.21.4的服务端。

# 新内容
# = 方块 =
* *[野花簇](野花簇.md)**
* 一种花，自然生成于[桦木森林](桦木森林.md)、[原始桦木森林](原始桦木森林.md)和[草甸](草甸.md)。
* 类似于[粉红色花簇](粉红色花簇.md)，同一格空间内最多可以容纳4株野花簇。
* 对野花簇[骨粉](骨粉.md)会生成更多野花簇。
* 有4种方向。
* 可以放入[堆肥桶](堆肥桶.md)中堆肥。
* 可以用于合成[黄色染料](黄色染料.md)。
* 能用于引诱、喂食[蜜蜂](蜜蜂.md)。

* *[枯叶堆](枯叶堆.md)**
* 一种装饰性方块，自然生成于[森林](森林.md)、[黑森林](黑森林.md)和[疏林恶地](疏林恶地.md)。
* 类似于粉红色花簇，同一格空间内最多可以容纳4个枯叶堆。
* 有4种方向。
* 有独特方块音效。
* 可由烧炼[树叶](树叶.md)获得，也可以作为燃料用于[烧炼](烧炼.md)。
* * 作为燃料用于烧炼时，每个枯叶堆可烧炼0.5个物品。
* 会随所处的生物群系着色。
* 可以被方块直接替换。
* 只能放置到[顶面支撑形状完整](判定箱#方块支撑形状.md)的方块上。

* *[萤火虫灌木丛](萤火虫灌木丛.md)**
* 会在[沼泽](沼泽.md)、[红树林沼泽](红树林沼泽.md)和大多数主世界生物群系的[水源](水源.md)附近自然生成。
* 在[内部天空光照](内部天空光照.md)≤13时，每刻发光的萤火虫粒子有70%的概率会在萤火虫灌木丛附近生成。
* 满足以下条件时，每刻有概率会发出特殊的环境音效：
* * 在[主世界](主世界.md)中。
* * 上方没有除[树叶](树叶.md)外的[阻止运动方块](阻止运动方块.md)。
* * 在一天的第12600至23400刻间。
* 对其[骨粉](骨粉.md)会在附近可生成位置生成新萤火虫灌木丛。
* 会发出等级为2的[光照](光照.md)。
* 可用于[堆肥](堆肥.md)，有30%的概率使堆肥层数增加一层。
* 能放置于具有<code>#dirt</code>标签的方块或[耕地](耕地.md)上。

* *[灌木丛](灌木丛.md)**
* 一种装饰性方块。会在以下生物群系以斑块形式生成：
* *
* *
* *
* *
* *
* *
* *
* *
* *
* 可用于[堆肥](堆肥.md)，有30%的概率使堆肥层数增加一层。
* 对其[骨粉](骨粉.md)会在附近可生成位置生成新灌木丛。
* 仅在被[剪刀](剪刀.md)或附有[精准采集](精准采集.md)魔咒的工具破坏时掉落。
* 可以被方块直接替换。
* 能放置于具有<code>#dirt</code>标签的方块或耕地上。

* *[矮枯草丛](矮枯草丛.md)和[高枯草丛](高枯草丛.md)**
* 自然生成于[沙漠](沙漠.md)和[恶地](恶地.md)及其变种生物群系。
* 都只有一格高。
* 与[枯萎的灌木](枯萎的灌木.md)一样能放置于[沙子](沙子.md)、[陶瓦](陶瓦.md)、[耕地](耕地.md)和[泥土](泥土.md)类方块上。
* 与[草丛](草丛.md)一样，在方块范围内具有随机偏移。
* 对其骨粉：
* * 矮枯草丛会生长为高枯草丛。
* * 高枯草丛在其周围的有效位置生成矮枯草丛。
* 可用于[堆肥](堆肥.md)，有30%的概率使堆肥层数增加一层。
* 可用作燃料。
* [绵羊](绵羊.md)可以食用它们以长出[羊毛](羊毛.md)。
* 可以被方块直接替换。
* 仅在被剪刀或附有精准采集魔咒的工具破坏时掉落。

* *[仙人掌花](仙人掌花.md)**
* 一种花，有概率在[沙漠](沙漠.md)和[恶地](恶地.md)及其变种生物群系的仙人掌柱上生成。
* 可放置于[仙人掌](仙人掌.md)、耕地或任何顶部可支撑其中心的方块上。
* 有概率由仙人掌长出：
* * 格高的仙人掌柱有10%概率长出仙人掌花且不再长高。
* * 3格及更高的仙人掌柱有25%概率长出仙人掌花。
* * 只有生成位置四面均无固体方块时，仙人掌花才会生成。
* 可用于[堆肥](堆肥.md)，有30%的概率使堆肥层数增加一层。
* 可用于引诱、喂食[蜜蜂](蜜蜂.md)。
* 可用于合成1个[粉红色染料](粉红色染料.md)。

* *[结构方块](结构方块.md)**
* <code>load</code>模式下的结构方块现在具有新选项<code>Strict Placement</code>。
* * 新选项为<code>true</code>时，被放置的结构中的方块将不会触发方块更新、方块实体副效果或形状更新。

* *[测试方块](测试方块.md)**
* 用于实现基于方块的测试的技术性方块。
* 物品[稀有度](稀有度.md)为“史诗”。
* 有4种模式：
* * 启动模式（<code>mode</code>为<code>start</code>）：在测试启动时产生一次红石脉冲。
* * 模式（<code>mode</code>为<code>log</code>）：接收到红石信号时，将消息输出到日志文件。
* * 失败模式（<code>mode</code>为<code>fail</code>）：接收到红石信号时，使测试失败。
* * 接受模式（<code>mode</code>为<code>accept</code>）：接收到红石信号时，成功完成这条路径的测试。
* 运行基于方块的测试需要结构中至少存在一个启动模式和一个接受模式的方块。

* *[测试实例方块](测试实例方块.md)**
* 用于运行、控制世界中的测试的技术性方块。
* 物品稀有度为“史诗”。
* 此方块表示世界中的测试实例。
* 可以用于与所对应的测试交互，以保存测试结构、重置测试或运行测试。
* 测试启动后，每个测试位置都会发出一条[信标光柱](信标光柱.md)以表示测试状态。
* * 灰色 = 运行中
* * 红色 = 失败（必要测试）
* * 橙色 = 失败（可选测试）
* * 绿色 = 成功
* 如果测试失败，错误会显示在一本讲台的书和一个遮罩上。
* 使用运行或创建了一个测试也将放置一个测试实例方块来控制此测试。

# = 物品 =
* *[蓝色鸡蛋](蓝色鸡蛋.md)**
* 由寒带鸡产下的[鸡蛋](鸡蛋.md)。
* 能生成幼年寒带鸡。
* 合成用途与普通鸡蛋相同。

* *[棕色鸡蛋](棕色鸡蛋.md)**
* 由热带鸡产下的[鸡蛋](鸡蛋.md)。
* 能生成幼年热带鸡。
* 合成用途与普通鸡蛋相同。

# = 生物 =
* *[猪](猪.md)、[牛](牛.md)和[鸡](鸡.md)**
* 加入了寒热带变种，即其变种由生成所处的[生物群系](生物群系.md)决定。
* * 繁殖出的幼年猪、牛或鸡的变种从其双亲的变种中随机选择。
* * 原本的猪、牛或鸡即为温带变种。
* * 三种鸡会生产对应的鸡蛋，以生成对应变种的幼年鸡。


* *[狼](狼.md)**
* 加入了6种音效变种，每种都有独特的空闲、受伤、死亡、嚎叫、呜咽和喘息音效。
* * 新的6种变种包括大型犬（big）、可爱（cute）、小巴哥犬（puglin）、愤怒（angry）、不爽（grumpy）和悲伤（sad），而原来的音效现在称为普通（classic）变种。
* * 每只狼都会从上述7种音效变种中随机选取一种使用，而与其纹理变种无关。

# = 世界生成 =
* *[地物](地物.md)**
* 加入了地物。

* *[随机斑块](随机斑块.md)**
* 加入了斑块。

* *[地物数据格式](地物数据格式.md)**
* 加入了地物类型<code>fallen_tree</code>。
* * 配置格式如下：
<div class="treeview">
  :*  根标签
  :** ：<code>fallen_tree</code>。
  :** ：此已配置的地物的配置。
  :*** ：作为树干和树桩的方块。
  :*** ：（）倒下的树干的长度。
  :*** ：在树桩上生成的装饰。
  :*** ：在倒下的树干上生成的装饰。
</div>

* 加入了<code>place_on_ground</code>树木装饰器。附加参数如下：
<div class="treeview">
  ** ：（默认为128，）尝试生成的次数。
  ** ：（默认为2，）尝试生成位置与树的根部的最大水平距离。
  ** ：（默认为1，）尝试生成位置与树的根部的最大垂直距离。
  ** ：作为装饰物的方块。如果生成位置为空气或下方与[完全固体渲染方块](判定箱#遮挡形状.md)相接的[藤蔓](藤蔓.md)，这个方块就能被放置。
</div>

* 加入了<code>attached_to_logs</code>树木装饰器。附加参数如下：
<div class="treeview">
  ** ：（）放置装饰物的概率。
  ** ：一个方块状态提供器，表示装饰器在原木上生成的方块。
  ** ：（不能为空）生成装饰物的方向。
</div>

* *[生物群系数据格式](生物群系数据格式.md)**
* 在生物群系的环境效果中加入了新字段，以定义为枯叶堆着色时所使用的颜色。

* *[结构](结构.md)**
* 加入了空结构。

* *[树木](树木.md)**
* 加入了倒下的树木，以同步。
* * 倒下的树木共有5种，分别是：橡树、白桦、高白桦、丛林和云杉树。
* * 有些倒下的树木附有[蘑菇](蘑菇.md)和[藤蔓](藤蔓.md)。
* * 除了[草甸](草甸.md)、[竹林](竹林.md)、[河流](河流.md)和[雪林](雪林.md)外，所有会生成对应立式树木的生物群系中均会生成倒下的树木。
* ** 例外是，[繁花森林](繁花森林.md)中会生成倒下的白桦，但不会生成倒下的橡树。

# = 游戏内容 =
* *[游戏规则](游戏规则.md)**
* 加入了新规则<code>[allowFireTicksAwayFromPlayer](游戏规则/allowFireTicksAwayFromPlayer.md)</code>，默认值为<code>false</code>。
* * 为<code>true</code>时，距离玩家128格外的区块中的[火](火.md)和[熔岩](熔岩.md)仍可执行[游戏刻](游戏刻.md)。
* 加入了<code>[tntExplodes](游戏规则/tntExplodes.md)</code>游戏规则，以同步。
* * 为<code>false</code>时，TNT将不能爆炸，也不可被激活。
* * 默认为<code>true</code>。

* *[粒子](粒子.md)**
* 加入了<code>tinted_leaves</code>粒子。
* * 具有参数<code>color</code>，可以为一个整数或一个4浮点数列表。
* 加入了<code>firefly</code>粒子。

* *[槽位](槽位.md)**
* 加入了<code>saddle</code>生物通用槽位。
* * 可装备于此槽位的物品由<code>equippable</code>物品堆叠组件控制。
* * 只有自然支持此槽位的生物能在装备对应物品时被[骑乘](骑乘.md)并在实体模型上渲染鞍。

# = 命令格式 =
* ***
* 用于创建和运行测试的命令。
* 注意：游戏测试的设计为运行于标准[超平坦](超平坦.md)世界。每次测试运行时，其周围的区域都会被石头代替，且测试会被屏障盒包裹。
* 语法如下：
* * <code>test clear*</code>：清除与所选的测试关联的结构和方块。
* ** <code>test clearall [<radius>]</code>
* ** <code>test clearthat</code>
* ** <code>test clearthese</code>
* * <code>test create</code>：在当前位置创建一个指定测试的测试设置，为指定大小的测试结构做准备。
* ** <code>test create <test> [<width>] [<height> <depth>]</code>
* * <code>test locate</code>：在已加载区块中寻找测试的位置。
* ** <code>test locate <selector></code>
* * <code>test reset*</code>：为所选的测试重置测试结构，移除周围的所有[屏障](屏障.md)并重新放置结构。
* ** <code>test resetclosest</code>
* ** <code>test reseetthese</code>
* ** <code>test resetthat</code>
* * <code>test pos</code>：显示测试中指向方块的局部坐标。如果指定了变量名，则在由单击此命令返回消息中的坐标获得的代码片段中使用该变量名。
* ** <code>test pos [<variable>]</code>
* * <code>test run*</code>：运行一个或多个测试。先加载每个测试的给定结构，再运行相关测试。如果一次运行多个测试，则它们会被放置于网格中并行运行，但有一定限制。如果超出限制，则测试按轮依次运行。
* ** <code>test run <selector> [<numberOfTimes>] [<untilFailed>] [<rotationSteps>] [<testsPerRow>]</code>
* ** <code>test runclosest [<numberOfTimes>] [<untilFailed>]</code>
* ** <code>test runfailed [<numberOfTimes>] [<untilFailed>] [<rotationSteps>] [<testsPerRow>]</code>
* ** <code>test runmultiple <selector> [<amount>]</code>
* ** <code>test runthat [<numberOfTimes>] [<untilFailed>]</code>
* ** <code>test runthese [<numberOfTimes>] [<untilFailed>]</code>
* * <code>test stop</code>：停止所有测试。由测试触发的游戏事件可能仍会继续。
* * <code>test verify <tests></code>：通过运行同一测试的多个实例来验证一个或多个测试。
* 参数：
* * <code>radius</code>：需要清除的测试的范围。
* * <code>test</code>：测试的命名空间ID。
* * <code>selector</code>：支持通配符（）的测试ID选择器。未提供命名空间时，默认使用<code>minecraft:</code>。
* ** 示例：
* *** <code>*:*</code> - 匹配所有ID。
* *** <code>*</code> - 匹配<code>minecraft</code>命名空间下的全部ID。
* *** <code>custom:folder/*_test_?</code> - 匹配<code>custom</code>命名空间下文件夹中为<code>-{}-<*任意前缀*>_test_<*单字符后缀*></code>的ID。
* * <code>width</code>：（默认为<code>5</code>）测试结构的宽度。
* * <code>height</code>：（默认使用<code>width</code>的值）测试结构的高度。
* * <code>depth</code>：（默认使用<code>width</code>的值）测试结构的深度。
* * <code>variable</code>：复制代码段中使用的变量名。
* * <code>numberOfTimes</code>：（默认为<code>1</code>）重复每个测试的次数。
* * <code>untilFailed</code>：（布尔值，默认为<code>false</code>）测试是否应该在一次迭代失败后立即停止。
* * <code>rotationSteps</code>：（默认为<code>0</code>）测试的额外90度旋转步数。
* * <code>testsPerRow</code>：（默认为<code>8</code>）网格布局中每行放置的测试数量。

# = 常规 =
* *[语言](语言.md)**
* 加入了对吉尔吉斯语的支持。

* *[数据包](数据包.md)**
* 加入了、、、、、、和子文件夹。
* 数据包现在可以用于配置测试实例与测试环境定义。
* [猫](猫.md)、[青蛙](青蛙.md)、[猪](猪.md)、[牛](牛.md)和[鸡](鸡.md)变种，以及狼的音效变种现在可由数据包定义。

* *[实体谓词](实体谓词.md)**
* 在实体谓词中加入了新字段。
* * 存在时，谓词会匹配实体组件的内容（类似于匹配物品堆叠的物品谓词的字段<code>components</code>）。
* * 格式：组件类型键与组件匹配值的映射。
* 加入了新可选字段<code>predicates</code>。
* * 除了操作对象是实体组件外，其功能和格式与物品谓词<code>predicates</code>相同。
* * 存在时，将匹配实体组件的部分内容。

* *[实体数据格式](实体数据格式.md)**
* 向所有实体（除固有此字段的[标记](标记.md)）加入了字段。
* * 由于以<code>custom_data</code>[数据组件](数据组件.md)形式公开，故可由生成类物品设置，也可由谓词匹配。
* * 当前自定义实体数据存储于<code>data</code>中，未来引入更合适的实体组件存储方式后，此字段名称将会更改。
* * 仅在非空时存储。

* *方块谓词**
* 加入了新可选字段。
* * 除了操作对象是方块实体组件（若有）外，其功能和格式与同名的物品谓词相同。
* * <code>components</code>存在时，其将匹配方块实体组件的精确内容。
* * <code>predicates</code>存在时，其将匹配方块实体组件的部分内容。

* *[物品堆叠组件](物品堆叠组件.md)**
* 加入了新组件。
* * 存在时，物品使用次数统计信息会在用此物品时增加。
* * 附加字段如下：
<div class="treeview">
  :*  物品堆叠组件
  :** ：（默认为1，）每次攻击对此物品造成的损伤值，即损耗的[耐久度](耐久度.md)。
  :** ：（，默认为<code>0</code>）非零时，用此武器进行的成功攻击将会禁用[盾牌](盾牌.md)指定秒数。
</div>
  * 示例：<code>weapon{item_damage_per_attack:2}</code>
* 加入了新组件，。
* * 存在时，具有组件的物品所储存效果的持续时长将随此组件所提供的倍率缩放。
* * 未指定此组件时，持续时长倍率默认为<code>1.0</code>。
* * 示例：<code>potion_duration_scale0.25</code>
* 向组件中加入了可选字段，表示[创造模式](创造模式.md)玩家能否使用此物品破坏方块，默认为<code>true</code>。
* 加入了一系列用于配置实体的变种、外观或其他方面的组件。
* * 当[刷怪蛋](刷怪蛋.md)、[生物桶](生物桶.md)、[画](画.md)和[物品展示框](物品展示框.md)等物品具有这些组件时，组件将对其生成的实体生效。
* * 对实体使用键获取刷怪蛋时，获取的刷怪蛋不会储存这些组件。
* * 新组件如下：
* ** ：可以为之一。
* ** ：可以为之一。
* ** ：可以为之一。
* ** ：可以为之一。
* ** ：可以为之一。
* ** ：可以为之一。
* ** ：可以为之一。
* ** ：可以为之一。
* ** ：可以为之一。
* ** ：可以为之一。
* ** ：16种染料颜色之一。
* ** ：16种染料颜色之一。
* ** ：16种染料颜色之一。
* ** ：16种染料颜色之一。
* ** ：16种染料颜色之一。
* ** ：16种染料颜色之一。
* ** ：需为取自<code>cat_variant</code>注册表的命名空间ID。
* ** ：需为取自<code>frog_variant</code>注册表的命名空间ID。
* ** ：需为取自<code>painting_variant</code>注册表的命名空间ID。
* ** ：需为取自<code>pig_variant</code>注册表的命名空间ID。
* ** ：需为取自<code>wolf_variant</code>注册表的命名空间ID。
* ** ：需为取自<code>cow_variant</code>注册表的命名空间ID。
* ** ：需为取自<code>chicken_variant</code>注册表的命名空间ID。
* ** ：需为取自<code>wolf_sound_variant</code>注册表的命名空间ID。
* * 创造模式物品栏中的生物桶和画现在使用新变种组件而非<code>minecraft:entity_data</code>组件。
* * 热带鱼桶和画物品的自定义提示框文本现在基于新组件而非。
* 加入了组件。
* * 玩家持拥有此组件的物品能像[盾牌](盾牌.md)一样阻挡攻击。
* * 格式如下：
<div class="treeview">
  :*  物品堆叠组件
  :** ：（，默认为<code>0</code>）成功阻挡攻击前需要按住右键的秒数。
  :** ：（，默认为<code>1</code>）被可停用阻挡的攻击击中时，物品冷却时长的乘数。为<code>0</code>时，此物品不能被攻击禁用。
  :** ：（伤害类型标签）指定标签的伤害类型可以无视此物品的阻挡而造成实际伤害。
  :** ：控制可阻挡多少伤害。未指定时，可阻挡一切伤害。
  :*** ：控制可挡下的伤害量和伤害类型。阻挡成功时，伤害减少<code>-{}-clamp(base + factor * *所受攻击伤害*, 0, *所受攻击伤害*)</code>。
  :**** ：固定阻挡的伤害。
  :**** ：应被阻挡的伤害比例。
  :**** ：可阻挡的伤害类型。允许单个ID、列表或标签。未指定则表示对所有伤害有效。
  :**** ：（，角度制，默认为90）在水平方向上，以当前玩家视角的水平分量向量为基准，如果受伤害方向与基准方向夹角小于此角度则伤害可被阻挡，否则不能阻挡。
  :** ：控制攻击对物品造成的耐久损耗。未指定时，每次攻击损耗物品1点耐久。物品耐久最终损耗<code>-{}-floor(threshold, base + factor * *所受攻击伤害*)</code>。最终值可以为负数以使物品修复。
  :*** ：（）攻击对此物品造成的最低耐久度损耗。
  :*** ：损耗物品固定耐久度。
  :*** ：所受攻击伤害的乘数。
  :** ：成功阻挡攻击时播放的声音事件。
  :** ：此物品被攻击停用时播放的声音事件。
</div>
* 加入了组件。
* * 存在时，物品因耐久耗尽而损坏时播放此声音事件。
* * 不存在时，物品损坏时不会播放任何音效。
* * 此组件是每个物品类型的默认组件之一。
* * 示例：<code>break_sound'item.wolf_armor.break'</code>
* 加入了组件，需为一个旗帜图案标签。
* * 存在时，物品可被放入[织布机](织布机.md)的图案槽位。
* * 示例：<code>provides_banner_patterns'#minecraft:pattern_item/globe'</code>
* 加入了组件，需为一个盔甲纹饰材料。
* * 存在时，物品将向盔甲纹饰配方提供指定的纹饰材料。
* * 在内置锻造配方中使用时，此物品必须在<code>#trim_material</code>标签中。
* * 因此，盔甲纹饰材料注册表定义不再指定物品ID。
* * 示例：<code>provides_trim_material'minecraft:iron'</code>
* 加入了组件，以隐藏由任何组件提供的提示框文本。
* * 因此，字段、和组件被取代。
* * 此组件是每个物品类型的默认组件之一。
* * 格式如下：
<div class="treeview">
  :*  物品堆叠组件
  :** ：（默认为<code>false</code>）物品提示框是否总是隐藏。
  :** ：（默认为<code>[]</code>）一个物品组件ID列表，列表内的所有组件提供的提示框文本都会被隐藏。如果组件不提供提示框文本，则对其没有效果。
  :*** ：一个物品堆叠组件ID。
</div>

* *[注册表](注册表.md)**
* 加入了固有注册表。
* 加入了可写注册表。

* *[标签](Java版标签.md)**
* 加入了下列方块标签：
* * ：。
* ** 蘑菇被放置或生长时可替换的方块。
* * ：。
* ** 能被剑瞬间破坏的可破坏方块。
* * ：。
* ** 会播放沙漠方块环境音效的方块。
* * ：。
* ** 可被[绵羊](绵羊.md)食用而使其羊毛生长的方块。
* * ：。
* ** 上方可以生成[骆驼](骆驼.md)的方块。
* 加入了下列物品标签：
* * ：。
* ** 可通过与成书合成来复制的物品。
* * ：。
* ** 不会被[鸡骑士](鸡骑士.md)捡起的物品。还用于合成[蛋糕](蛋糕.md)和[南瓜派](南瓜派.md)的配方。
* * ：。
* ** 现有的方块标签的对应物品标签。
* 加入了下列生物群系标签：
* * ：。
* ** 可生成寒带变种的绵羊、猪、鸡、牛（统称为家畜）的生物群系。
* * ：。
* ** 可生成热带变种的绵羊、猪、鸡、牛的生物群系。
* 加入了下列实体标签：
* * ：。
* ** 可以装备[鞍](鞍.md)的实体。该标签仅用于鞍的物品默认组件，不控制其实际功能。
* * ：。
* ** 表示会在其马类实体[物品栏](物品栏.md)中显示马铠[槽位](槽位.md)的实体。

* *[装备模型](装备模型.md)**
* 加入了预设模型层。

* *[物品模型映射](物品模型映射.md)**
* 加入了枚举属性<code>component</code>，用于返回物品的组件值（如有）。
* * 附加字段如下：
* ** ：[物品堆叠组件](物品堆叠组件.md)类型的命名空间ID。
* * 可能的值取决于物品堆叠组件类型。例如类型设置为<code>minecraft:wolf/collar</code>时，枚举值列表会接受染料颜色（如<code>lime</code>）。
* * 如果可能的值来自注册表但当前数据包未提供此值，则此表项将被静默忽略。
* 加入了布尔属性<code>component</code>：
* * 使用物品组件谓词来匹配物品组件。
* ** 类似于[物品子谓词](物品子谓词.md)，但是，不是将它们编码为<code>-{}-<*谓词类型*>:<*谓词值*></code>的映射，而是将单个谓词编码为两个字段：
<div class="treeview">
  ::** ：组件谓词类型。可以为中之一。
  ::** ：谓词指定的值。
</div>

* *[颜色图](纹理#颜色图.md)**
* 加入了，用于枯叶堆类可着色方块的着色。

* *[资源包](资源包.md)**
* 为绵羊羊毛加入了底层纹理，以在游戏中动态着色。

* *[客户端核心文件](客户端核心文件.md)**
* 加入了新命令行传入参数<code>--renderDebugLabels</code>。
* * 为相关OpenGL对象添加调试标签，使开发者更容易调试渲染。

* *[服务端核心文件](服务端核心文件.md)**
* 在中加入了<code>net.minecraft.gametest.Main</code>入口点，用于自动启动运行全部可用测试项目的服务端。
* * 选项如下：
* ** ：显示用法信息。
* ** {{cd|-{}---packs <*路径*>}}：设置世界中启用的数据包路径（可以为包含文件的文件夹或<code>.zip</code>格式的压缩包）。
* ** {{cd|-{}---report <*路径*>}}：将类似于JUnit的XML报告导出到给定路径。
* ** {{cd|-{}---tests <*选择*>}}：指定根据选择表达式（与命名空间测试实例ID匹配的通配符表达式）运行的测试内容。如果此项省略或指定空ID，则运行所有测试项。
* ** {{cd|-{}---universe <*路径*>}}：（默认为<code>gametestserver</code>）设置服务端世界存放路径。会替换已有的文件夹。
* ** {{cd|-{}---verify <*布尔值*>}}：（默认为<code>false</code>）启用测试验证。启用时，对每个90度旋转步骤运行以<code>test</code>或<code>testNamespace</code>指定的测试100次。
* * 用法示例：<code>java -DbundlerMainClass"net.minecraft.gametest.Main" -jar server.jar --packs mytestpacks</code>

* *[测试实例定义格式](测试实例定义格式.md)**
* 游戏测试实例是定义运行测试的小型资产，可以在数据包注册表<code>test_instance</code>中定义。
* 测试类型分为2种：内置函数测试和基于方块的测试。
* * 内置函数测试依赖内置函数来运行测试并指示成功或失败。
* * 基于方块的测试使用测试结构中的[测试方块](测试方块.md)来运行测试并指示成功或失败。
* 游戏内置一个永远成功的测试实例<code>minecraft:always_pass</code>。
* 格式如下：
<div class="treeview">
  *  父标签
  ** ：作为测试一部分的测试环境的命名空间ID。
  ** ：用于测试的结构的命名空间ID。
  ** ：测试的类型。需为之一。
  ** ：（）允许测试通过而不超时的最大刻数。
  ** ：（默认为<code>0</code>，）放置测试结构后、测试启动前需要等待的刻数。
  ** ：（默认为<code>1</code>，）尝试运行此测试的次数。
  ** ：（默认为<code>1</code>，）此测试必须成功的次数。
  ** ：（默认为<code>true</code>）对要通过的完整测试套件而言，这个测试是否必须通过。
  ** ：（默认为<code>false</code>）此测试是否不包含于自动测试运行组中。
  ** ：（默认为<code>false</code>）此测试是否需要天空。为<code>false</code>时，测试结构被屏障完全包裹；为<code>true</code>时，屏障盒顶部敞开。
  ** ：（默认为<code>none</code>）对测试结构应用的旋转角度。可以为之一。
  ** 为<code>function</code>时，附加以下字段：}}
  ** ：要运行的测试函数的命名空间ID。目前内置的测试函数只有<code>minecraft:always_pass</code>。
</div>

* *[测试环境定义格式](测试环境定义格式.md)**
* 游戏测试环境是对测试进行分组并为其提供正确的运行前提条件的一种方法，可以在数据包注册表<code>test_environment</code>中定义。
* 游戏内置一个空测试环境<code>minecraft:default</code>。
* 格式如下：
<div class="treeview">
  *  父标签
  ** ：测试环境的类型。需为之一。
  ** 为<code>all_of</code>时，附加以下字段：}}
  ** ：子定义设置列表，其中的定义均会应用。
  ** 为<code>function</code>时，附加以下字段：}}
  ** ：用于安装的mcfunction文件的命名空间ID。
  ** ：用于卸载的mcfunction文件的命名空间ID。
  ** 为<code>game_rules</code>时，附加以下字段：}}
  ** ：布尔型游戏规则的设置列表。
  *** ：一个游戏规则与一个设置值。
  **** ：要设置的规则的精确ID。测试环境完成后，此规则会被重置为默认值。
  **** ：规则的设置值。
  ** ：整型游戏规则的设置列表。
  *** ：一个游戏规则与一个设置值。
  **** ：要设置的规则的精确ID。测试环境完成后，此规则会被重置为默认值。
  **** ：规则的设置值。
  ** 为<code>weather</code>时，附加以下字段：}}
  ** ：要设置的天气。需为之一。
  ** 为<code>time_of_day</code>时，附加以下字段：}}
  ** ：（）要设置的当日时间，单位为刻。
</div>

* *游戏测试框架**
* 现在可以通过修改游戏代码的方式在<code>TEST_FUNCTION</code>注册表中添加测试函数，以使用比基于方块的测试方式更通用的游戏测试框架。

* ***
* 具有生成规则的变种现在使用统一方法选择，过程如下：
* * 每个变种字段的每一表项包含一个条件和一个整数优先级
* * 给定实体类型的所有变种的条件将根据生成实体的位置进行评估，然后游戏从剩余优先级最大的表项中随机选取一项
* * 如果不存在任何条件，则变种保持默认值
* 格式如下：
<div class="treeview">
  * ：通用变种选择器。
  **
  *** ：选中此条件的优先级。
  *** ：一项生成条件。
  **** ：生成条件类型。如果此字段不存在，则条件始终为真。
  **** 为<code>biome</code>，则匹配生成位置的生物群系，附加下列字段：}}
  **** ：单个生物群系ID、列表或生物群系标签。
  **** 为<code>moon_brightness</code>，则匹配生成位置的[月亮亮度](月亮#月亮亮度.md)，附加下列字段：}}
  **** ：单个浮点数或如<code>{"min": 1, "max": 2}</code>一样的对象。
  ***** ：最小亮度值。
  ***** ：最大亮度值。
  **** 为<code>structures</code>，则匹配生成位置是否在结构中，附加下列字段：}}
  **** ：单个结构ID、列表或结构标签。
</div>

* *[猫变种定义格式](猫变种定义格式.md)和[青蛙变种定义格式](青蛙变种定义格式.md)**
* 猫的变种可通过{{filepath|-{}-data/<*命名空间*>/cat_variant/<*id*>.json}}来添加。
* 青蛙的变种可通过{{filepath|-{}-data/<*命名空间*>/frog_variant/<*id*>.json}}来添加。
* 文件格式如下：
<div class="treeview">
  *  父标签
  ** ：此变种纹理资产的命名空间ID，会被解析为{{filepath|-{}-assets/<*命名空间*>/textures/<*路径*>.png}}。
  ** ：[通用变种选择器](#通用变种选择器.md)。
</div>
* 这两项特性均属于实验性功能。

* *[猪变种定义格式](猪变种定义格式.md)、[牛变种定义格式](牛变种定义格式.md)和[鸡变种定义格式](鸡变种定义格式.md)**
* 猪的变种可通过{{filepath|-{}-data/<*命名空间*>/pig_variant/<*id*>.json}}来添加。
* 牛的变种可通过{{filepath|-{}-data/<*命名空间*>/cow_variant/<*id*>.json}}来添加。
* 鸡的变种可通过{{filepath|-{}-data/<*命名空间*>/chicken_variant/<*id*>.json}}来添加。
* 文件格式如下：
<div class="treeview">
  *  父标签
  ** ：此变种纹理资产的命名空间ID，会被解析为{{filepath|-{}-assets/<*命名空间*>/textures/<*路径*>.png}}。
  ** ：此变种使用的模型。
  ** ：[通用变种选择器](#通用变种选择器.md)。
</div>
* 这三项特性均属于实验性功能。

* *[狼音效变种定义格式](狼音效变种定义格式.md)**
* 狼的音效变种可通过{{filepath|-{}-data/<*命名空间*>/wolf_sound_variant/<*id*>.json}}来添加。
* 格式：
<div class="treeview">
  *  父标签
  ** ：狼空闲音效的命名空间ID，或直接定义一个[声音事件](Java版声音事件.md)。
  ** ：狼死亡音效的命名空间ID，或直接定义一个声音事件。
  ** ：狼嚎叫音效的命名空间ID，或直接定义一个声音事件。
  ** ：狼受伤音效的命名空间ID，或直接定义一个声音事件。
  ** ：狼喘息音效的命名空间ID，或直接定义一个声音事件。
  ** ：狼呜咽音效的命名空间ID，或直接定义一个声音事件。
</div>

* *[纹理图集](纹理图集.md)**
* 向<code>paletted_permutations</code>纹理图集源中加入了新字段：
* * ：（默认为<code>_</code>）连接原始纹理和置换调色板以生成精灵图名称时使用的分隔符。

# 更改
# = 方块 =
* *[大型垂滴叶](大型垂滴叶.md)**
* 不再能放置于[苍白苔藓块](苍白苔藓块.md)上。

* *[气泡柱](气泡柱.md)**
* 现在不受其影响的实体，在里面产生的粒子和音效更少。

* *[嘎枝之心](嘎枝之心.md)**
* 为在合理结构中但由于白天而处于休眠状态的嘎枝之心加入了新纹理。
* * 休眠状态的嘎枝之心不能生成嘎枝，也不能生成[树脂团](树脂团.md)。
* 现在激活与休眠状态的转换取决于固定的时间，而不再受天气和维度亮度影响。
* 方块状态<code>active</code>现在被<code>creaking_heart_state</code>取代。
* * 可能的值为。

* *[眼眸花](眼眸花.md)**
* 现在状态的转换取决于固定的时间，而不再受天气和维度亮度影响。

* *[火](火.md)**
* 默认情况下，周围8个区块内不含玩家时，火不再燃烧或蔓延。

* *[铁块](铁块.md)、[铁栏杆](铁栏杆.md)、[铁活板门](铁活板门.md)、[铁门](铁门.md)和[重质测重压力板](重质测重压力板.md)**
* 加入了新的破坏、放置、在上方移动、摔落和打击音效。

* *[橡树树叶](橡树树叶.md)、[丛林树叶](丛林树叶.md)、[金合欢树叶](金合欢树叶.md)、[深色橡树树叶](深色橡树树叶.md)、[云杉树叶](云杉树叶.md)、[红树树叶](红树树叶.md)、[白桦树叶](白桦树叶.md)、[杜鹃树叶](杜鹃树叶.md)和[盛开的杜鹃树叶](盛开的杜鹃树叶.md)**
* 现在有概率产生对应颜色的落叶粒子。

* *[苍白橡树树苗](苍白橡树树苗.md)和[苍白橡树树叶](苍白橡树树叶.md)**
* 地图基色由更改为。

* *[磁石](磁石.md)**
* 更改了配方，现在需要1个[铁锭](铁锭.md)和8个[雕纹石砖](雕纹石砖.md)合成。
* * 此配方现在会在玩家获得铁锭或磁石时解锁。
* 现在可以在主世界和下界的[废弃传送门](废弃传送门.md)的战利品箱子中找到个磁石。

* *[树脂砖台阶](树脂砖台阶.md)**
* <code>type</code>状态为<code>double</code>的树脂砖台阶现在会正常掉落2个树脂砖台阶而非1个。

* *[信标](信标.md)**
* 信标及其光柱的渲染距离现在可以超过16个区块，直到客户端设置的渲染距离。
* * 相机离光柱越远，光柱将渲染得越厚，以使在远处也能发现光柱。
* * 信标光柱的最大渲染高度也从1024格提升到了2048格。

* *[沙子](沙子.md)、[红沙](红沙.md)和所有[陶瓦](陶瓦.md)**
* 现在当任意3个方向上8格外也存在上述方块时会发出[环境音效](环境音效.md)。
* * <code>block.sand.idle</code>会在所有生物群系中播放；在[沙漠](沙漠.md)和[恶地](恶地.md)中，还可能播放<code>block.sand.wind</code>。

* *[枯萎的灌木](枯萎的灌木.md)**
* 现在当下方两格均为沙子、红沙或陶瓦时会发出环境音效。
* 现在可在耕地上放置。

* *[细雪](细雪.md)**
* 穿着[皮革靴子](皮革靴子.md)摔落到其上的实体，如果摔落高度大于2.5格，则会穿过细雪而不被其接住。

* *[草丛](草丛.md)**
* 降低了其音效文件（挖掘）的音量。

* *[眼眸花](眼眸花.md)和[苍白垂须](苍白垂须.md)**
* 产生的闲置音效分类现在由“方块”改为“”。

* *[熔岩](熔岩.md)和[水](水.md)**
* 产生的环境音效分类现在由“方块”改为“”。

* *[活塞](活塞.md)**
* 现在所破坏的方块会播放对应的破坏音效。

* *[移动的活塞](移动的活塞.md)**
* 方块实体数据中存储的不再允许指定为空对象。

* *[末地折跃门方块](末地折跃门方块.md)**
* 方块实体数据中的不再在被移除时保留。

* *[熔炉](熔炉.md)、[烟熏炉](烟熏炉.md)和[高炉](高炉.md)**
* 方块实体数据中的不再在被移除时保留。

* *[头颅](头颅.md)**
* 方块实体数据中的不再在被移除时保留。

* *[营火](营火.md)**
* 和字段不再在被移除时保留。

* *[雕纹书架](雕纹书架.md)**
* 字段现在具有默认值<code>-1</code>。

* *[漏斗](漏斗.md)**
* 字段现在具有默认值<code>-1</code>。

* *[拼图方块](拼图方块.md)**
* 、和字段现在具有默认值<code>minecraft:empty</code>。
* 现在具有默认值<code>minecraft:air</code>。

* *[幽匿尖啸体](幽匿尖啸体.md)**
* 字段现在具有默认值<code>0</code>。

* *[结构方块](结构方块.md)**
* 和字段现在具有默认值<code>true</code>。
* 字段现在具有默认值<code>1</code>。

# = 物品 =
* *[蜂巢](蜂巢（方块）.md)和[蜂箱](蜂箱.md)**
* 现在其方块物品默认具有<code>block_state</code>组件。

* *[弩](弩.md)**
* 现在其物品提示框中会显示装填的所有物品，而非只有首个物品。
* * 其中装填的所有物品也会将它们的完整提示框以分段形式加入。
* * 相同弹射物的提示框文本会合并。

* *[容器](容器.md)**
* 所有容器方块物品现在会像[潜影盒](潜影盒.md)一样在提示框中显示其中的前5个物品（若有）。

* *[烟花火箭](烟花火箭.md)**
* 现在会合并提示框中的多个相同[烟火之星](烟火之星.md)的提示框文本。

* *[地图](地图.md)**
* 没有<code>map_id</code>组件的地图不再在其提示框中显示“”。
* * 无效ID仍会产生此提示。

* *[收纳袋](收纳袋.md)**
* 现在可以在[村庄](村庄.md)的战利品箱子中找到。具体位于：
* * 各生物群系村庄房屋
* * 村庄皮匠小屋
* * 村庄制图师小屋
* * 村庄武器匠小屋

* *[刷怪蛋](刷怪蛋.md)**
* 更新了纹理。
* * 现在刷怪蛋的大小与所生成生物的大小相匹配。
* * 现在刷怪蛋的外观会凸显出所生成生物的外观。

# = 生物 =
* *[骆驼](骆驼.md)**
* 现在会在[沙漠](沙漠.md)中自然生成。

* *[牛](牛.md)和[哞菇](哞菇.md)**
* 更新了模型。
* * 现在会渲染其鼻子。
* * 现在腿部模型是镜像的。
* 字段不再在被移除时保留。

* *[嘎枝](嘎枝.md)**
* 现在与嘎枝之心绑定的嘎枝也能被[命名牌](命名牌.md)命名。
* * 被命名的嘎枝不会因为白天而消失。

* *[僵尸猪灵](僵尸猪灵.md)**
* 现在只会在被玩家杀死时掉落玩家独有战利品。

* *[绵羊](绵羊.md)**
* 绵羊的羊毛颜色现在取决于其生成所处的生物群系。

* 现在绵羊身上的毛斑块会保留剪毛前染的颜色，以匹配。
* 现在可以食用[蕨](蕨.md)，以匹配。
* 调整了羊毛纹理，以消除背部的明显线条。

* *[可骑乘生物](骑乘.md)**
* 现在装备于其身上的[鞍](鞍.md)在掉落时能保留诸如自定义名称之类的物品属性。

* *[玩家](玩家.md)**
* 存档崩溃报告中，现在还可能包含玩家的UUID、坐标、游戏模式和权限等级。

* *[村民](村民.md)**
* 实体数据中的不再在移除时保留。
* 、、、和字段现在具有默认值<code>0</code>。

* *[流浪商人](流浪商人.md)**
* 实体数据中的不再在移除时保留。
* 字段现在具有默认值<code>0</code>。

* *[狼](狼.md)**
* 实体数据中的字段现在默认为<code>14</code>（红色）而非<code>0</code>（白色）。

* *[悦灵](悦灵.md)**
* 移除了实体数据中冗余的字段。

* *[猫](猫.md)**
* 实体数据中的字段现在默认为<code>14</code>（红色）而非<code>0</code>（白色）。

* *[苦力怕](苦力怕.md)**
* 字段现在具有默认值<code>30</code>。
* 字段现在具有默认值<code>3</code>。

* *[海豚](海豚.md)**
* 移除了实体数据中的、和字段。
* 字段现在具有默认值<code>2400</code>。

* *[末影龙](末影龙.md)**
* 字段现在具有默认值<code>0</code>。

* *[末影人](末影人.md)**
* 不再允许指定为空对象。

* *[恶魂](恶魂.md)**
* 字段现在具有默认值<code>1</code>。

* *[山羊](山羊.md)**
* 和字段现在具有默认值<code>true</code>。

* *[狐狸](狐狸.md)**
* 字段现在具有默认值<code>[]</code>。
* 用修改字段时，不再会将其与旧状态合并。

* *[幻翼](幻翼.md)**
* 将字段重命名为。
* 、和字段现已合并为字段。

* *[潜影贝](潜影贝.md)**
* 字段现在具有默认值<code>16</code>（未染色）。

* *[骷髅](骷髅.md)**
* 字段不再在被移除时保留。

* *[雪傀儡](雪傀儡.md)**
* 字段现在具有默认值<code>true</code>。

* *[行商羊驼](行商羊驼.md)**
* 字段现在具有默认值<code>47999</code>。

* *[海龟](海龟.md)**
* 、和字段现已合并为字段。
* 移除了、和字段。
* 将字段重命名为。

* *[恼鬼](恼鬼.md)**
* 将字段重命名为。
* 、和字段现已合并为字段。

* *[僵尸](僵尸.md)类生物**
* 字段不再在被移除时保留。

* *[僵尸村民](僵尸村民.md)**
* 字段不再在移除时保留。
* 字段现在具有默认值<code>0</code>。
* 字段不再在被移除时保留。

* *[灾厄村民](灾厄村民.md)和[劫掠兽](劫掠兽.md)**
* 字段不再在移除时保留。

* *非玩家生物数据**
* 字段现已合并为字段。
* * 格式为每个盔甲槽位与物品堆叠的映射。
* * 有效的盔甲槽位包括：。
* * 此字段不存在时，实体将无盔甲。

* *可驯服的生物**
* <code>Owner</code>字段不再必须调用玩家。

* *实体数据**
* 字段现在已合并为字段。
* * 格式为装备槽位类型与概率值的映射，有效的装备槽位包括。
* * 如果此字段未指定或被移除，则掉落概率为默认值<code>0.085f</code>。
* * 值默认的掉落概率字段不再被存储；如果内所有字段均为默认值，则此字段会被移除。
* * 示例：<code>drop_chances:{chest:0.1,feet:1.0}</code>
* 现在改为。

# = 非生物实体 =
* *[区域效果云](区域效果云.md)**
* 为区域效果云实体数据加入了新字段，默认为<code>1.0</code>。
* * 表示其中药水效果持续时间的缩放系数。
* * [滞留药水](滞留药水.md)生成的区域效果云的缩放系数为<code>0.25</code>。
* 字段现在具有默认值<code>-1</code>。
* 现在当字段为<code>-1</code>时，区域效果云将永不自然消失。
* * 这代表未指定持续时间而生成的区域效果云不再立刻消失。

* *[下落的方块](下落的方块.md)**
* 实体数据中的字段现在可以为<code>air</code>，并会导致其立刻消失。
* 现在在未指定或无效时默认为<code>sand</code>。
* 不再允许指定为空对象。
* 字段现在具有默认值<code>false</code>。但当为[铁砧](铁砧.md)时，此值默认为<code>true</code>。
* 字段现在具有默认值<code>0</code>。
* 字段现在具有默认值<code>40</code>。
* 字段现在具有默认值<code>true</code>。
* 字段不再在被移除时保留。

* *[物品实体](物品实体.md)**
* 在地面上时，模型大小现在会影响悬停运动。
* * 因此，模型大小不会使其卡进下方的方块中。
* 物品堆叠在地面上悬停的位置现在取决于模型大小和模型类型。
* * 模型深度（Z轴）小于方块的时，物品显示为平面堆叠。
* * 否则，模型将渲染为一组从中心全方向偏移的物品。
* * 之前，平面堆叠只在使用父级模型时渲染。
* 实体数据和字段不再在被移除时保留。
* 字段现在具有默认值<code>5</code>。
* 字段现在具有默认值<code>0</code>。
* 字段现在具有默认值<code>0</code>。

* *[被激活的TNT](被激活的TNT.md)**
* 现在在未指定或无效时默认为<code>tnt</code>。
* 不再允许指定为空对象。
* 字段现在具有默认值<code>80</code>。
* 字段现在具有默认值<code>4</code>。

* *[潜影弹](潜影弹.md)**
* 和不再在移除时保留。

* *[唤魔者尖牙](唤魔者尖牙.md)、[区域效果云](区域效果云.md)和所有[弹射物](弹射物.md)**
* 实体数据字段不再在被移除时保留。

* *[物品展示框](物品展示框.md)和[荧光物品展示框](荧光物品展示框.md)**
* 、和字段现已合并为字段。
* 字段现在具有默认值<code>1.0</code>。

* *[画](画.md)和[拴绳结](拴绳结.md)**
* 、和字段现已合并为字段。

* *[箭类弹射物](弹射物#箭类弹射物.md)**
* 和字段不再在被移除时保留。
* 不再允许指定为空对象。

* *[矿车](矿车.md)及其变种**
* 移除了冗余的字段。
* 已指定的现在会覆盖默认展示的方块状态。
* 不再允许指定为空对象。
* 现在即使没有自定义展示方块状态设置，也可以指定来覆盖默认偏移量。

* *[展示实体](展示实体.md)**
* 字段不再在被移除时保留。
* 方块展示实体的不再允许指定为空对象。
* 现在变换规则的渲染效果与手持时相同。
* 、和字段现在具有默认值<code>0</code>。
* 字段现在具有默认值<code>1</code>。
* 字段现在具有默认值<code>0</code>。
* 字段现在具有默认值<code>1</code>。
* 和字段现在具有默认值<code>0</code>。

* *[喷溅药水](喷溅药水.md)和[滞留药水](滞留药水.md)**
* 移除了<code>potion</code>实体类型。现在代表喷溅药水和滞留药水的实体被拆分为。

* *[烟花火箭](烟花火箭.md)**
* 字段现在具有默认值<code>false</code>。

* *[交互实体](交互实体.md)**
* 和字段现在具有默认值<code>1</code>。

* *[光灵箭](光灵箭.md)**
* 字段现在具有默认值<code>200</code>。

* *[TNT矿车](TNT矿车.md)**
* 字段现在具有默认值<code>80</code>。
* 字段现在具有默认值<code>4</code>。
* 字段现在具有默认值<code>1</code>。

* *[箭类弹射物](箭类弹射物.md)**
* 字段现在具有默认值<code>2</code>。

* *[火球类弹射物](火球类弹射物.md)**
* 字段现在具有默认值<code>0.1</code>。

* *[火球](火球.md)**
* 字段现在具有默认值<code>1</code>。

# = 世界生成 =
* *[恶地](恶地.md)**
* 现在[绵羊](绵羊.md)、[猪](猪.md)、[鸡](鸡.md)和[牛](牛.md)会在恶地及其生物群系变种自然生成。

* *[返回传送门](返回传送门.md)**
* 如果(0,0)处没有方块，现在返回传送门会在比维度最低位置高1格处生成，而非在最低点生成。
* 尝试放置返回传送门方块时，现在会使被替换的方块掉落。

* *[甘蔗斑块](随机斑块.md)**
* 不再生成于[深暗之域](深暗之域.md)、[溶洞](溶洞.md)、[雪林](雪林.md)和[积雪山坡](积雪山坡.md)。

* *[巨型蘑菇](巨型蘑菇.md)**
* 放置蘑菇方块时，现在只能取代空气及<code>#replaceable_by_mushrooms</code>方块标签内的方块。

* *[草甸](草甸.md)**
* 现在[矮草丛](矮草丛.md)的生成更加稀疏。

* *[苍白之园](苍白之园.md)**
* 更改了生成，现在会更多地取代黑森林的生成空间。

* *[林地府邸](林地府邸.md)**
* 现在能在苍白之园中生成。

# = 游戏内容 =
* *[交易](交易.md)**
* 对制图师和[流浪商人](流浪商人.md)交易项的平衡性调整不再属于[实验性内容](实验性内容.md)。
* * 更改了流浪商人的交易项：
* ** 流浪商人的交易项现在由以下顺序和数量组成：
* *** 2个收购选项
* *** 2个特殊售货选项
* *** 5个普通售货选项
* ** 新增了收购选项，加入并更改了一些售货选项，详见下表：
  ::
  ::
  * 学徒制图师现在会根据[生物群系](生物群系.md)着装出售7种新的[地图](地图.md)。
  ** 各村庄地图指向对应生物群系的[村庄](村庄.md)，丛林探险家地图指向[丛林神庙](丛林神庙.md)，沼泽探险家地图指向[沼泽小屋](沼泽小屋.md)。
  ::
  * 专家制图师现在会根据生物群系着装出售不同颜色的[旗帜](旗帜.md)。
  ::
  :* 沼泽和丛林着装的[村民](村民.md)一般不会自然生成，需要玩家在对应生物群系繁殖村民或者治愈对应生物群系生成的[僵尸村民](僵尸村民.md)。
  ::

* 目前对[图书管理员](图书管理员.md)和[盔甲匠](盔甲匠.md)交易项的平衡性调整仍然属于[实验性内容](实验性内容.md)。

* *[进度](进度.md)**
* 进度“”现在移动到冒险根进度后。

* *[魔咒](魔咒.md)**
* 现在[荆棘](荆棘.md)造成伤害时会正常播放<code>player_hurt</code>音效事件。

* *[槽位](槽位.md)**
* 移除了<code>horse.saddle</code>鞍槽位。

* *[摔落](摔落.md)**
* 存储时和部分计算中的摔落高度现在使用双精度浮点数而非单精度浮点数。

* *[移动](移动.md)**
* 由于考虑到的修复对跑酷、和移动造成了太多副作用，因此回退了这项修复。

* *[创造模式物品栏](创造模式物品栏.md)**
* 刷怪蛋一栏的图标现在由[猪刷怪蛋](猪刷怪蛋.md)改为[苦力怕刷怪蛋](苦力怕刷怪蛋.md)。
* 现在[涡流纹样陶片](涡流纹样陶片.md)位于探险纹样陶片之后。

# = 命令格式 =
* ***
* 现在接受新选项<code>strict</code>，以在放置方块时不触发自身及紧挨着的方块的方块更新和形状更新。
* * 新语法：<code>place template <template> [<pos> [<rotation> [<mirror> [<integrity> [<seed> [strict]?]?]?]?]?]?</code>

* ***
* 现在接受新选项<code>strict</code>，以在放置方块时不触发自身及紧挨着的方块的方块更新和形状更新。
* * 新语法：<code>clone [from <sourceDimension>]? <begin> <end> [to <targetDimension>]? <destination> [strict]? [<nowiki/>[replace|masked|filtered <filter>] [force|move|normal]?]?</code>

* ***
* 现在可以透明地创建和修改异构列表。
* 不再支持用空键遍历路径，如不再受支持。

* ***
* 处理方块实体数据的方式发生了如下更改：
* * 如果未指定方块实体数据且当前目标方块有数据，则方块实体数据会被保留。
* * 如果指定了方块实体数据，则将目标方块实体数据设置为指定值。
* ** 若要清除方块实体数据，现在必须指定<code>{}</code>。
* * 如果方块状态或方块实体数据发生了改变，则操作视为成功。
* 现在接受新选项<code>strict</code>，以在放置方块时不触发自身及紧挨着的方块的方块更新和形状更新。
* 命令中的<code>replace</code>选项现在可以在后面附加额外选项。
* 新语法：
* * <code>fill <from> <to> <block> keep</code>
* * <code>fill <from> <to> <block> [replace <filter>]? [strict|destroy|hollow|outline]?</code>

* ***
* 处理方块实体数据的方式发生了如下更改：
* * 如果未指定方块实体数据且当前目标方块有数据，则方块实体数据会被保留。
* * 如果指定了方块实体数据，则将目标方块实体数据设置为指定值。
* ** 若要清除方块实体数据，现在必须指定<code>{}</code>。
* * 如果方块状态或方块实体数据发生了改变，则操作视为成功。
* 现在接受新选项<code>strict</code>，以在放置方块时不触发自身及紧挨着的方块的方块更新和形状更新。
* 新语法：<code>setblock <pos> <block> [destroy|keep|replace|strict]?</code>

* ***
* 指定<code>styled</code>数字格式时的文本格式现在也需为SNBT而非JSON格式。

* *[文本组件](文本组件.md)**
* 现在、和中的文本组件会以<code>@s</code>为执行者[解析](文本组件#组件解析.md)。

* *[SNBT格式](SNBT格式.md)**
* SNBT现在接受异构列表，即接受不同类型元素的列表。
* * 为保持兼容性，NBT格式未变化。
* * 从SNBT转换到NBT时（如作为或的参数），会将异构列表转换为可被NBT格式接受的形式。
* ** 例如，<code>['a', {'b':3}]</code>会存储为<code>[{'<nowiki/>': 'a'},{'b':3}]</code>。
* ** 从NBT转换到SNBT时（如使用显示），上述转换不会逆向进行。
* 扩展了命令（如NBT、[文本组件](文本组件.md)、[谓词](谓词.md)等）中描述类对象数据的文本格式。
* * **数字格式**
* ** 浮点数的整数或小数部分现在可以省略。
* *** 例如，现在是有效的。
* ** 浮点数现在可以使用E计数法。
* *** 例如，现在都可以用于表示<code>1200.0</code>。
* ** 整数现在可以添加<code>0x</code>前缀来表示16进制数，也可以添加<code>0b</code>前缀来表示2进制数。
* *** 例如，<code>0xbad</code>（等于2989）、<code>0xCAFE</code>（等于51966）以及<code>0b101</code>（等于5）。
* ** 整数现在不能以<code>0</code>为开头。
* *** 通常这表示8进制数，但现在将其限制以避免意外使用。
* ** 数字现在可以在数字序列之间（但不能在序列的开头或结尾）包含<code>_</code>字符。
* *** 例如，
* ** <code>NaN</code>、<code>Inf</code>或16进制浮点数不受支持。
* ** 扩展了类型后缀：
* *** 整型后缀（代表字节的、代表短整数的、代表整数的、代表长整数的等）现在可以加上<code>s</code>（有符号）或<code>u</code>（无符号）。
* *** 新后缀仅在解析时影响有效范围：值仍然以带符号的形式存储。
* **** 例如，<code>240ub</code>等同于<code>-16sb</code>，而<code>240sb</code>不会被解析。
* *** 没有符号时，默认十进制数有符号，而二进制数和十六进制数无符号。
* *** 注：由于<code>b</code>也是一个有效的十六进制数，单位字节的十六进制值只能随符号后缀写入，如。
* * **字符串**
* ** 引号字符串现在可以使用以外的转义序列：
* *** Unicode转义：
* **** <code>\x</code>：2位数转义，如<code>\x42</code>。
* **** <code>\u</code>：4位数转义，如<code>\u2603</code>。
* **** <code>\U</code>：8位数转义，如<code>\U00002603</code>。
* **** <code>-{}-\N{<*名称*>}</code>：按名称转义Unicode字符，如<code>\N{Snowman}</code>。
* *** 内置转义序列：
* **** <code>\b</code>：退格，对应Unicode转义为<code>\x08</code>。
* **** <code>\s</code>：空格，对应Unicode转义为<code>\x20</code>。
* **** <code>\t</code>：水平制表符，对应Unicode转义为<code>\x09</code>。
* **** <code>\n</code>：换行，对应Unicode转义为<code>\u0a</code>。
* **** <code>\f</code>：换页，对应Unicode转义为<code>\u0c</code>。
* **** <code>\r</code>：回车，对应Unicode转义为<code>\u0d</code>。
* ** 未加引号的字符串现在不能以开头，以避免与数字的意外冲突。
* * **数组**
* ** 现在没有后缀的数组（）中的值会假定其具有与数组类型相匹配的后缀。
* *** 例如，<code>[B;1,2]</code>等价于<code>[B; 1b, 2b]</code>。
* ** 数组现在可以接受比数组类型更小的值类型。
* *** 例如，<code>[I;1b,2s,3]</code>现在有效等价于<code>[I;1i,2i,3i]</code>。
* * **列表**
* ** 现在接受末尾的唯一逗号。
* *** 例如，<code>[1,2,]</code>现在有效等价于<code>[1,2]</code>。
* ** 出现在开头和中间的逗号都不被允许，例如均无效。
* * **复合标签**
* ** 复合标签（映射）现在接受末尾的唯一逗号。
* *** 例如，<code>{a:b,}</code>现在有效等价于<code>{a:b}</code>。
* ** 出现在开头和中间的逗号都不被允许，例如{{cd|{,}|{a:b,,}|d=pretty}}均无效。
* 为简化数据输入和使用，SNBT现在接受以下操作：
* * <code>bool(-{}-<*参数*>)</code>：将参数转换为布尔值。
* ** 参数为布尔值时，直接返回值。
* ** 参数为数字时，在非0时返回<code>true</code>，否则返回<code>false</code>。
* * <code>uuid(-{}-<*字符串*>)</code>：将[UUID](UUID.md)的字符串表示转换为整型数组。
* 将溢出至无穷的浮点值（如<code>1e1000</code>）现在会被拒绝。

* *[NBT格式](NBT格式.md)**
* 游戏中任何带有NBT数据的接口（SNBT表示，）现在都支持异构列表，即元素不属于同一类型的列表。
* * 插入或替换不同类型的列表不再报错。
* * 数组（如<code>[I;1,2,3]</code>）仍被严格限制。
* * 这意味着用于表示异构列表的“包装器”对象不再可通过游戏内现象观测。
* NBT文件的存储格式仍未改变。
* * 异构列表会在存储之前进行转换，以绕过NBT约束。

# = 常规 =
* *[进度定义格式](进度定义格式.md)**
* <code>background</code>字段不再使用<code>textures/</code>前缀和<code>.png</code>后缀。

* *区块加载**
* 由传送门加载的区块现在会在重新打开世界时重新加载。

* *[区块刻](区块刻.md)**
* 现在更改了部分区块刻的执行条件。
* * 雨雪相关逻辑、子区块内的[随机刻](随机刻.md)不再只在玩家128格内的区块中执行。
* * [雷暴](雷暴.md)相关逻辑、[周期生成](周期生成.md)仍需在玩家128格内的区块执行。
* 区块刻的执行现在需要实体计算等级（计算等级≤31）而非方块计算等级（计算等级≤32），即游戏不再使弱加载区块执行区块刻。

* *[全景图](全景图.md)**
* 更新了主菜单全景图以匹配[春意盎然](春意盎然.md)。

* *[数据包](数据包.md)**
* 将数据包版本号更改为。

* *[文本组件](文本组件.md)**
* 文本组件现在不再存储为JSON字符串。
* * 现在文本组件数据格式可以直接内联到外部结构中。
* ** 示例：<code>minecraft:custom_name"{\"text\":\"Renamed item\"}"</code>会变为<code>minecraft:custom_name{text:'Renamed item'}</code>。
* * 诸如和这些命令的参数也随之变化，如：{{cmd|tellraw @s {"text":"Hello world"}|link=none}}会变为{{cmd|tellraw @s {text:'Hello world'}|link=none}}。
* * 注：文本组件嵌入NBT时，布尔值会被视为数字。若要将布尔值参数传递到翻译，则需要使用字符串形式。
* * <code>nbt</code>类型和<code>interpret</code>设置为<code>true</code>的文本组件，将直接按给定的nbt解析文本组件，而非先转换为字符串再解析为JSON。
* 更新了悬停事件格式：
* * <code>hoverEvent</code>字段现在重命名为<code>hover_event</code>。
* * （曾由已渲染的文本组件解析而来的）遗留的<code>value</code>字段不再受支持。
* * 为<code>show_text</code>时：
* ** <code>contents</code>字段已被重命名为<code>value</code>。
* *** 示例：<code>{action:'show_text',contents:'Hello world'}</code>变为了<code>{action:'show_text',value:'Hello world'}</code>。
* * 为<code>show_item</code>时：
* ** 移除了<code>contents</code>字段。其中字段现在已内联。
* *** 示例：<code>{action:'show_item',contents:{id:'minecraft:stick',count:2}}</code>变为了<code>{action:'show_item',id:'minecraft:stick',count:2}</code>。
* ** 如果<code>contents</code>仅指定了物品ID，则现在会被内联替换为完整格式。
* *** 示例：<code>{action:'show_item',contents:'minecraft:stick'}</code>变为了<code>{action:'show_item',id:'minecraft:stick'}</code>。
* * 为<code>show_entity</code>时：
* ** 移除了<code>contents</code>字段。其中字段现在已内联。
* ** <code>id</code>字段已被重命名为<code>uuid</code>。
* ** <code>type</code>字段已被重命名为<code>id</code>。
* *** 示例：<code>{action:'show_entity',contents:{id:[I;0,0,0,0],type:'minecraft:pig'}}</code>变为了<code>{action:'show_entity',uuid:[I;0,0,0,0],id:'minecraft:pig'}</code>。
* 更新了点击事件格式：
* * <code>clickEvent</code>字段现在重命名为<code>click_event</code>。
* * 为<code>open_url</code>时：
* ** 现在<code>value</code>字段已被重命名为<code>url</code>。
* *** 如果URL值并非带有<code>https://</code>或<code>http://</code>方案的有效URI，则点击事件将不再解析而非仅失效。
* ** 示例：<code>{action:'open_url',value:'https:/<nowiki/>/minecraft.net'}</code>变为了<code>{action:'open_url',url:'https:/<nowiki/>/minecraft.net'}</code>。
* * 为<code>run_command</code>时：
* ** 现在<code>value</code>字段已被重命名为<code>command</code>。
* *** 如果命令中包含被禁止的字符，则点击事件将不再解析而非仅失效。
* *** 命令不再总是需要<code>/</code>前缀。
* ** 示例：<code>{action:'run_command',value:'/say Hi'}</code>变为了<code>{action:'run_command',command:'/say Hi'}</code>。
* * 为<code>suggest_command</code>时：
* ** 现在<code>value</code>字段已被重命名为<code>command</code>。
* *** 如果命令中包含被禁止的字符，则点击事件将不再解析而非仅失效。
* ** 示例：<code>{action:'suggest_command',value:'/help'}</code>变为了<code>{action:'suggest_command',command:'/help'}</code>。
* * 为<code>change_page</code>时：
* ** 现在<code>value</code>字段已被重命名为<code>page</code>。
* *** <code>page</code>字段现在需要正整数而非字符串。
* ** 示例：<code>{action:'change_page',value:'1'}</code>变为了<code>{action:'change_page',page:1}</code>。

* *[实体谓词](实体谓词.md)**
* 现在在目标实体不在地面上时恒返回False。

* *[实体子谓词](实体子谓词.md)**
* 移除了变种实体子谓词。
* * 其效果已被<code>components</code>谓词与变种实体组件的组合取代。
* 字段已从实体子谓词<code>sheep</code>中移除。

* *[配方](配方.md)**
* 对<code>crafting_transmute</code>配方类型作出如下更改：
* * <code>result</code>现在支持指定数量和附加组件以修改结果物品。
* ** <code>result</code>的新格式如下：
<div class="treeview">
  ::* ：物品的命名空间ID或一组结果物品对象。
  ::** ：物品的命名空间ID。
  ::** ：（默认为<code>1</code>，）物品数量。
  ::** ：（默认为空）附加组件。
</div>
  :* 示例：<code>"result": {"id": "minecraft:stick", "count": 3, "components": {"!minecraft:damage": {}, "minecraft:enchantment_glint_override": {}}}</code>
* <code>smithing_transform</code>配方中的<code>base</code>字段现在是必选的。
* <code>smithing_trim</code>配方：
* * 字段现在是必选的。
* * 加入了字段，控制配方中应用哪个纹饰图案。
* ** 示例：<code>"pattern": "minecraft:bolt"</code>。
* 类型转化配方现在不再在输入与输出物品具有相同ID但不完全相同时不生效。

* *[物品堆叠组件](物品堆叠组件.md)**
* 组件现有以下更改：
* * 字段现可设置为<code>saddle</code>，表示鞍槽位。
* * 现在，拥有该组件的物品可以被穿戴在任何满足的生物的<code>body</code>槽位中。
* * 加入了可选字段，默认为<code>false</code>，控制玩家是否可给生物穿戴该物品。
* ** 如为<code>true</code>，对该组件允许的生物该物品时，如相应槽位没有其他物品，即可为生物穿戴该物品。
* 移除了<code>hide_additional_tooltip</code>组件。
* * 世界升级时，此组件将被组件取代，由此组件隐藏的组件提示框将加入列表中，这些组件包括：
* **
* 组件中的字段已被移除，现在它们总是使用简化形式。
* 移除了组件中的字段。
* 移除了<code>hide_tooltip</code>组件。
* 对实体应用的组件现在会从物品转移到所生成的实体上：
* * <code>custom_name</code>→任何实体
* * <code>potion_contents</code>→滞留药水→区域效果云
* * <code>potion_duration_scale</code>→滞留药水→区域效果云

* *[战利品表函数](战利品表函数.md)**
* <code>toggle_tooltips</code>的<code>toggles</code>映射表中现在支持任意组件ID。
* * 现在会在<code>tooltip_display</code>组件中切换所指定的组件。

* *[盔甲纹饰定义格式](盔甲纹饰定义格式.md)**
* 不再包含字段。

* *[复述功能](复述功能.md)**
* 音量现在受“”分类下“”选项的影响。

* *[注册表](注册表.md)**
* 注册表现在变为可写注册表。
* 移除了注册表。

* *[实体数据格式](实体数据格式.md)**
* 项数不正确的、和（分别需要3、3、2项组件）现在会被完全弃用，而不再只选择已指定的组件。
* 被移除的字段不再保留。
* 实体的若未被指定，则默认为最大值。

* *[生物](生物.md)数据格式**
* 不再在被移除时保留。
* 若未被指定，则默认为最大值。
* 、和字段现已合并为字段。
* * 示例：<code>sleeping_pos: [I;1,2,3]</code>。

* *[玩家数据格式](玩家数据格式.md)**
* 装备于盔甲槽位（包括副手）的物品现在也会在中储存。
* * 这些物品在中的字段会被移除，并转换为中[槽位](槽位.md)名称的对应字段（包括）。
* 、、、、和字段现已合并为字段。
* * 格式：
<div class="treeview">
  :* ：玩家重生数据。
  :** ：玩家重生点所处的方块位置。
  :** ：（默认为<code>0.0</code>）玩家重生时的角度。
  :** ：（命名空间ID）玩家重生点所处的维度。
  :** ：（默认为<code>false</code>）由命令设置重生点时为<code>true</code>。
</div>
* 将更改为，为一个双精度浮点数列表。
* * 示例：<code>entered_nether_pos: [1.0, 2.0, 3.0]</code>。

* *[标签](Java版标签.md)**
* 移除了猫变种标签。
* 将方块标签重命名为了，并加入了。
* 结构标签不再属于[实验性内容](实验性内容.md)“”。
* 向伤害类型标签中加入了。
* 向方块标签中加入了。
* 向方块标签中加入了。
* 向方块标签中加入了。
* 从方块标签中移除了可被[瞬间破坏](瞬间破坏.md)的方块。

* *[袭击存储格式](袭击存储格式.md)**
* 重命名以下字段：
* * <code>Raids</code>→<code>raids</code>
* * <code>Tick</code>→<code>tick</code>
* * <code>NextAvailableID</code>→<code>next_id</code>
* * <code>Id</code>→<code>id</code>
* * <code>Started</code>→<code>started</code>
* * <code>Active</code>→<code>active</code>
* * <code>TicksActive</code>→<code>ticks_active</code>
* * <code>BadOmenLevel</code>→<code>raid_omen_level</code>
* * <code>GroupsSpawned</code>→<code>groups_spawned</code>
* * <code>PreRaidTicks</code>→<code>cooldown_ticks</code>
* * <code>PostRaidTicks</code>→<code>post_raid_ticks</code>
* * <code>HeroesOfTheVillage</code>→<code>heroes_of_the_village</code>
* * <code>TotalHealth</code>→<code>total_health</code>
* * <code>NumGroups</code>→<code>group_count</code>
* * <code>Status</code>→<code>status</code>
* 将袭击中心坐标、、合并为。

* *[方块实体数据格式](方块实体数据格式.md)**
* 字段不再在被移除时保留。
* 字段不再在被移除时保留。

* *[资源包](资源包.md)**
* 将资源包版本号更改为。
* 更改了<code>pig_saddle</code>纹理的尺寸。
* 与鞍相关的纹理现在被移动或拆分到了{{filepath|-{}-textures/entity/equipment/*<实体>*-saddle/saddle.png}}，以便[装备模型](装备模型.md)调用。
* * 这些纹理路径中的现在已分别改为。
* 将温带猪纹理重命名为，并更改了纹理尺寸。
* 将温带牛纹理重命名为，并更改了纹理尺寸。
* 更改了红色和棕色哞菇的纹理尺寸。
* 将温带鸡纹理重命名为，并从移动到。
* 现在被重命名为。
* 移除了和。
* 狼原来的音效文件现在移入文件夹。
* 移除了狼未使用的音效。
* 向[Programmer Art](Programmer Art.md)中加入了25w08a更新前刷怪蛋的纹理。

* *[装备模型](装备模型.md)**
* 配置了的装备模型层的显示不再要求物品处于<code>#dyeable</code>物品标签中。

* *[着色器](着色器.md)和后处理管线**
* 移除了核心着色器的着色器程序定义文件和后处理效果的JSON文件。
* * 着色器程序本身仍可用且可被覆写。
* * 后处理效果仍可用JSON文件配置。
* 后处理渲染过程JSON格式中的现被和取代。
* * <code>-{}-<*命名空间*>:<*路径*></code>会被解析为{{filepath|-{}-assets/<*命名空间*>/shaders/<*路径*>.<vshfsh>}}。
* 每个Uniform定义的字段现在是必选项。
* * 现在可选值包括。
* 每个Uniform定义的字段现在是可选项，但不建议不设置此项。
* * 不设置此项用于模糊效果的运行库配置。
* 着色器将使用的每个Uniform都需要指定，否则会导致未定义的行为。
* “全局”Uniform现在可以在任何着色器中定义，游戏将尝试填充这类变量。
* * 目前，包含这些Uniform：。
* * Uniform的类型必须与其通常应有的类型匹配，否则将导致未定义的行为。

* *[Realms](Realms.md)**
* 将世界上传到Realms现在会默认启用命令方块。
* 现在能将极限模式单人游戏世界上传到Realms。

* *[截图](截图.md)**
* 移除了未使用的巨型屏幕截图功能。

* *网络协议**
* 发向客户端的<code>player_chat</code>数据包现在包含一个随着信息发送到客户端递增的索引。
* * 每次登录或在配置阶段由登录数据包重置时索引从0开始。
* * 对于每条消息，服务端会将此值增加1。
* * 如果此值以未预定的方式更新，客户端会断开连接。
* * 由于该协议需要每个聊天数据包按生成顺序到达客户端，此功能目的是自定义服务器开发人员能更快地检查丢失或被重新排序的聊天信息。
* 发向服务端的<code>chat</code>和<code>chat_command_signed</code>数据包现在在<code>last seen</code>更新中包含一个校验和字节。
* * 这是<code>last seen</code>签名的简单哈希值，应由服务端重建，以允许快速检查不同步状态。
* * 可以传递0以禁用检查，和协议转换相兼容。

* *常规**
* 如果游戏检测到上次启动未正常完成，则会重置全屏选项。

# 修复
命令产生错误信息且在获取结果或是否成功时返回0。
|56653|处于发怒状态的僵尸猪灵被任何因素杀死都会掉落经验和稀有掉落物。
|80243|鞍不对装备其的实体应用属性修饰符。
|90212|用鞘翅滑行时不能悬挂到可攀爬方块上。
|93185|末地返回传送门在(0,0)的最高方块处生成，导致其可能生成得不完整。
|97244|<code>minecraft:enchant.thorns.hit</code>声音事件不在非玩家实体被荆棘魔咒伤害时播放。
|98271|持盾牌格挡的音效只能被其他玩家（不包含格挡的人）听到。
|101556|下界传送门的传送范围过大（相当于一个完整的方块）。
|108495|非生物实体可同处一队（显示颜色）但不能用<code>team</code>选择。
|113878|应用于胸甲槽位的属性修饰符描述存在误导：“When on body”而非“When on chest”。<!-- 保持原文，译文暂未更改 -->
|117574|使用和替换方块实体且未指定NBT时，旧NBT会被保留；即使命令执行失败，容器物品栏也会被清空。<!--
|118053|<code>Owner</code>为非玩家实体的狼生来坐着。Reopened-->
|118092|仅解锁一个配方时，合成配方解锁弹窗也称“New Recipes Unlocked!”。<!-- 保持原文，英文单复数问题 -->
|118470|音量为“关”时，复述以满音量播放。
|122840|“”无法移除末地水晶的<code>beam_target</code>标签。
|128079|剪刀的统计信息不会在挖掘特定方块时增加。
|135192|如果水面上存在一根线，船就会立刻下沉。
|141297|不能用、调试棒和<code>BlockStateTag</code>将红石火把的<code>lit</code>状态设置为相反值。
|152382|末地折跃门和末地传送门不会随渲染距离雾淡出。
|153392|无法移除村民言论。
|157196|已驯服的动物不能加入队伍。
|160304|冰上潜行能使移动方向与X/Z轴平行。
|165421|气泡柱的效果对风弹相反。
|165461|装载后，即使仍按住右键，弩的装载动画也会停止。
|165711|末影之眼碰撞箱总延迟到动画后。
|167125|摔落伤害被错误地取整。
|168262|枯萎的灌木不能放置于耕地。
|170134|Minecraft在探索一段时间地形后使用超出所需量数倍的VRAM。
|176233|装载后，弩不再播放过渡动画。
|177522|狼的嚎叫声与哀鸣声均配以字幕“”。
|183329|玩家带有反胃状态效果时，下界传送门传送遮罩不会出现。
|183623|选择世界菜单中的极限模式世界的红色感叹号后存在逗号。
|184530|玩家低速移动时的移动方向会偏向基本方向。
|184681|20w21a后，自定义的世界仍称作“”。
|186241|“”画质下世界边界面渲染异常。
|189525|自1.9之前的版本升级的世界中的穿装备的实体双持盔甲。
|190000|装备于马的鞍不能被其他鞍直接替换。
|193404|用调试棒或不能修改动力或激活铁轨的充能状态。
|200311|进度背景纹理引用与模型不一致。
|202226|单个配方也称作“recipes”。
|203550|被活塞破坏的方块不会播放破坏音效。
|220091|召唤<code>falling_block</code>实体时，设为任意空气方块（）的<code>BlockState</code>NBT会被改为默认值<code>sand</code>。
|220672|自定义世界预设标题源字符串存在不一致的单复数拼写。
|222876|“Buffet world customization”<!-- 保持原文 -->存在错误的大小写。
|226772|潜影盒的开启/关闭字幕显示“Shulker”。<!-- 保持原文，译文无此问题 -->
|227097|末地传送门删除了传送门中的方块。
|230445|末地传送门和末地折跃门未在失明或黑暗效果下合适地渲染。
|231331|进度被撤销后，其获取弹窗仍继续出现。
|236100|末地水晶光柱似乎是黑色的。
|239705|面向特定角度时向前疾跑会使玩家向左或向右偏转。
|241951|玩家在X和Z轴上的动量分别在其数值较低时被取消。
|248099|行走的生物意外落水后，其寻路损坏了。
|253721|执行时出现错误日志。
|255756|玩家导致盾牌损坏时，攻击者听不到盾牌损坏音效。
|256469|隐形的骆驼不显示其鞍。
|257133|在小空间中放置带有NBT的大型画时失去同步。
|258253|传入刷怪蛋的粒子使用未着色的纹理。
|260440|正在用鞘翅飞行的玩家不再能在水中激活游泳模式。
|261385|气泡柱表面的行为受非空气方块影响而存在差异。
|262928|<code>minecraft:entity.player.hurt</code>不再在玩家受到来自守卫者或远古守卫者的荆棘伤害时播放。
|262939|在反胃状态效果生效期间切换维度或重生会显示下界传送门遮罩。
|265514|在1.20.2更新中，头相关转移函数仍在定向音频被设为“关”时启用。
|266912|将鞍装备到刚生成的马、驴、骡或骆驼时，装备鞍的音效不会播放。
|267221|<code>change_page</code>点击事件Json中的<code>value</code>需要一个字符串而非整数。
|267323|物品的旧JSON文本式的布尔型字段会在世界更新时被清除。
|268942|在用鞘翅滑翔的同时游泳时，游泳动画不再显示。
|269386|创造模式物品栏中的涡流纹样陶片未处于合适的位置。
|269637|马能穿戴多个鞍，但不能装备多个马铠。
|270043|减少盔甲的光效时，也减少了三叉戟的光效。
|270192|某些实体的鞍在其死亡动画中一直可见。
|270197|猪和炽足兽身上的鞍在其死亡动画中一直可见。
|270220|赋予所有进度产生持续很久的嘈乱音频。
|272100|马的纹理（）中存在未使用的调色像素。
|272790|末地返回传送门中的潜影盒和其他方块未在传送门改变状态时作为物品掉落。
|273338|用线和上升气泡柱能产生飞行船故障。
|274187|禁用PVP时，来自附有火焰附加的剑的横扫攻击能使其他玩家着火。
|274258|所有马的纹理都包含一个未使用的箱包纹理。
|275994|在窗口不聚焦时开着饼图会频繁产生日志。
|276061|用无陶片饰纹陶罐替换其他饰纹陶罐时，只要不重进，陶片类型就不会更新。
|276861|玩家高速移动时偶尔能传送过被堵住的末地传送门。
|277403|燃烧的箭和三叉戟仍不会因放置于同处的水而灭火。
|277537|苍白覆地苔藓偶尔生成得脱节。
|277780|即使上次打过补丁，Minecraft也会在每次自动保存时重新保存所有地图。
|277807|传送玩家的坐骑会导致不同步，但之前不会这样。
|277865|中的<code>Time</code>大于某特定值时，气泡柱中的船的动画损坏。
|277922|在一格高下行气泡柱中的船会继续来回摆动。
|278096|“”画质和被禁用的云导致所有粒子在帧缓冲区中停留一帧。
|278249|经验球的位置严重不同步，然后突然自我修正。
|278375|给羊驼或行商羊驼疯狂喂食小麦或干草捆导致物品堆叠失去同步。
|278376|箭和三叉戟在一格深的气泡柱中出现怪异行为。
|278400|箭和三叉戟击中在流动的熔岩中的方块后继续移动。
|278417|插在地上的箭和三叉戟上的火不能被雨熄灭。
|278455|旁观模式下，向非接近45°的倍数的角度侧向移动错误。
|278493|已驯服的狼对嘎枝的敌对状态在这个嘎枝消失后仍然维持。
|278502|垂泪藤或缠怨藤出现断开问题。
|278615|即使一击杀死了召唤支-{}-援的僵尸猪灵，其支-{}-援者仍与你敌对。
|279340|幼年北极熊太小了。
|279390|幼年僵尸猪灵的-{}-头盔穿模。
|279646|用切换全屏不会更新视频设置菜单中的全屏选项。
|280170|除非游戏规则mobGriefing被设为<code>false</code>，山羊不再能冲撞盔甲架。
|;1.21.4的漏洞
|278040|沉底的嘎枝的游泳寻路损坏了。
|278140|在放置相邻的有方向性状态的方块时运行不正常。
|278164|点击设置按钮且接下来的屏幕中的鼠标处存在滚动条时，点击音效播放两次。
|278204|烈焰人火球现在会导致TNT矿车立刻爆炸。
|278422|在用移除一只坐着且已驯服的狼的<code>Owner</code>标签后重载世界会导致这只狼坐着移动。
|278552|从高处下落时，<code>minecraft:entity.player.big_fall</code>不再播放音效。
|278585|摔落到细雪边缘的玩家会卡住。
|278591|对末影龙实体使用时不再给予末影龙刷怪蛋。
|278621|落地前放置细雪导致客户端/服务端不同步。
|278627|穿着皮革靴子摔落到细雪边缘的玩家会受到摔落伤害。
|278634|尝试对发光地衣、树脂团、幽匿脉络等方块“<code>waterloggedtrue</code>”状态会报错。
|278639|绊线钩不会被潜影弹触发。
|278659|攻击被玩家观察的嘎枝不会触发幽匿感测体。
|278673|的相对于实体原始角度的X轴旋转角度限制于±90。
|278683|由刷怪笼或生成的生物永不掉落其装备。
|278708|在骑乘状态中投掷末影珍珠只会脱离坐骑，而不会传送。
|278728|<code>smithing_transform</code>配方不能把物品的组件改回默认值。
|278733|破坏一个树脂砖双层台阶方块只会返回一个台阶。
|278734|绵羊向上/下看时，其头部动作卡顿。
|278755|骑乘实体时远程传送会导致游戏软锁。
|278801|闭合的眼眸花合成为灰色染料的配方未与其他灰色染料配方共组。
|278841|细雪不再减速从高处下落的玩家。
|279145|潜影弹不再受气泡柱影响。
|279152|潜影弹不能通过下界/末地传送门和末地折跃门传送。
|279196|无法用移除方块战利品表。
|279364|方块实体的<code>CustomName</code>无法移除。
|279472|虚空看起来比之前更低、更淡了。
|279572|末地传送门和末地折跃门不受水、熔岩或细雪雾影响。
|279653|执行命令后，方块实体数据失去同步。
|279857|召唤的猪灵不会拾取金质物品。
|280211|<code>glDebugVerbosity</code>设为3时，末地水晶光柱会导致OpenGL报错。
}}

# 参考

# 导航

[de:1.21.5](de:1.21.5.md)
[en:Java Edition 1.21.5](en:Java Edition 1.21.5.md)
[es:Java Edition 1.21.5](es:Java Edition 1.21.5.md)
[fr:Édition Java 1.21.5](fr:Édition Java 1.21.5.md)
[it:Java Edition 1.21.5](it:Java Edition 1.21.5.md)
[ja:Java Edition 1.21.5](ja:Java Edition 1.21.5.md)
[lzh:爪哇版一點二一點五](lzh:爪哇版一點二一點五.md)
[pl:Java Edition 1.21.5](pl:Java Edition 1.21.5.md)
[pt:Edição Java 1.21.5](pt:Edição Java 1.21.5.md)
[ru:1.21.5 (Java Edition)](ru:1.21.5 (Java Edition).md)
[th:รุ่น Java 1.21.5](th:รุ่น Java 1.21.5.md)
[uk:1.21.5 (Java Edition)](uk:1.21.5 (Java Edition).md)