# 基岩版声音事件

*页面ID: 143921* | *来源: Minecraft Wiki*

* *声音事件（Sound Event）**是游戏播放声音使用的基础数据对象。

# 绑定声音
当一个或多个游戏内事件触发了一个声音事件时，声音系统会根据的内容来决定播放哪个音效。是一个由[资源包](资源包.md)的声音系统使用的文件，存储在中。

# = <samp>sound_definitions.json</samp> =
定义了声音事件与本地文件的关系。
<div class="treeview">
* : 根对象。
* * ：当前值为，表示自[村庄与掠夺](村庄与掠夺.md)以来使用的标准格式。
* * ：对声音事件的定义。
* ** ：单个声音事件，通常会分为一类（例如或）。默认的声音事件会在下方列出。
* *** ：控制声音音量滑条的分类。。默认值为。
* *** ：声音开始衰减的距离。默认值为0。
* *** ：声音完全听不见的距离。默认值为16。
* *** ：声音事件所使用的具体音效。
* **** ：该音效在资源包中的路径（不包含拓展名）。若详细定义了每个音效的具体参数，则会使用下方的组件。
* ****
* ***** ：该音效在资源包中的路径（不包含拓展名）。
* ***** ：当该声音为流式播放时为true。默认值为false。
* ***** ：是否以立体声播放。
* ***** ：声音的音量。默认值为1.0。可以是一个区间以表示范围。
* ***** ：声音的音高。默认值为1.0。可以是一个区间以表示范围。
* ***** ：声音的权重，即触发当前声音事件后该音效会被播放的概率，例如值为2时相当于将该音效重复定义了1次。权重默认为1。
* ***** ：是否预载。预载的音效可以在低内存设备上播放。
* *** ：该声音事件[隐藏式字幕](隐藏式字幕.md)的本地化键名。
</div>

# = <samp>music_definitions.json</samp> =
定义了生物群系或特定游戏场景内与声音事件的关系。
<div class="treeview">
* : 根对象。
* * ：生物群系或特定游戏场景名。
* ** ：声音事件的[命名空间ID](命名空间ID.md)。
* ** ：声音事件触发后经过多长时间才会播放（最大值），单位为秒。默认值为0。
* ** ：声音事件触发后经过多长时间才会播放（最小值），单位为秒。默认值为0。
</div>

# = <samp>sounds.json</samp> =
定义了游戏事件与声音事件的关系。
<div class="treeview">
* : 根对象。
* * ：声音事件的集合名。值可以是。
* ** ：二级集合名，用于表示某一类方块的共同音效集合，例如和。
* *** ：声音的音量。默认值为1.0。可以是一个区间以表示范围。
* *** ：声音的音高。默认值为1.0。可以是一个区间以表示范围。
* *** ：具体的声音事件[命名空间ID](命名空间ID.md)的集合。
* **** ：系统音效类型，例如。默认值和声音事件命名空间ID保持一致。
* ***** ：单个声音事件命名空间ID，例如。作为简写，该参数有时候会直接以命名空间ID为值。
* ****** ：声音的音量。默认值为1.0。可以是一个区间以表示范围。此值与二级集合的同一参数为乘积关系，以表示该声音最终的音量。
* ****** ：声音的音高。默认值为1.0。可以是一个区间以表示范围。此值与二级集合的同一参数为乘积关系，以表示该声音最终的音高。
</div>

# 播放声音
一个声音事件是和游戏事件联系在一起的。多个声音事件可能对应不同的游戏事件或属于不同的声音分类，但它们共用同一个声音文件。小部分的有效声音文件并没有出现于，而其他则没有相对应的游戏事件。任何缺少游戏事件的声音事件都仅能通过命令播放。

# 历史

|、和。}}
}}

# 参见
* [Sounds.json/基岩版数据值](Sounds.json/基岩版数据值.md) —— 原版的声音事件定义文件。

# 导航

[Category:资源包](Category:资源包.md)