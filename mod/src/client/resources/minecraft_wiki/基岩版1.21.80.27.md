# 基岩版1.21.80.27

*页面ID: 149341* | *来源: Minecraft Wiki*

<onlyinclude>

* *beta 1.21.80.27**（ChromeOS/Android）、**Preview 1.21.80.27**（Xbox/Windows/iOS/iPadOS）是[基岩版1.21.80](基岩版1.21.80.md)的第5个测试版，发布于2025年4月8日<ref></ref>，修复了一些漏洞。

# 新内容
# = 常规 =
* *[设置](设置.md)**
* 在Xbox和Nintendo Switch平台上的设置中加入了“”选项。

# 更改
# = 常规 =
* *[实验性玩法](实验性玩法.md)**
* 移除了“”子选项。

# = 技术性 =
* *API**
* 的版本
* * 将方法由移动至。
* * 将接口由移动至。
* * 将接口由移动至。
* * 将错误类由移动至。
* 的版本
* * 协程（承诺）现在会在早期执行中刷新，以允许异步导入以与如自定义组件和自定义命令等早期执行API交互。
* * 加入了对自定义命令枚举的支持。
* * 为加入了类型。
* * 的方法不再支持在参数内使用生成事件。
* ** 接口现在有一个可选的字符串成员，用于指定实体生成时发送给该实体的生成事件。
* ** 参数内任何现有的生成事件需要更新为使用选项。
* *** 例如：{{cd|spawnEntity("minecraft:horse<minecraft:ageable_grow_up>", {x:0, y:0, z:0})}}应该变为{{cd|spawnEntity("minecraft:horse", {x:0, y:0, z:0}, {spawnEvent: 'minecraft:ageable_grow_up'})}}。
* * 移除了。
* * 的方法现在会为中使用注册的自定义组件返回。
* 的版本
* *
* ** 将属性的类型由 number  string)[]}}更改为 number  string  undefined)[]}}。
* *** 新签名： number  string  undefined)[]}}。
* *** 旧签名： number  string)[]}}。
* ** 数组现在包含由请求提供的所有元素的项。这意味着标签、标头和分隔符会被视为未定义值。

* *方块组件**
* 修复了中的各同向性纹理会被挤压和拉伸的问题。

* *组件**
* 内容日志错误现在会记录物品组件中缺失的实体引用。

* *[基岩版编辑器](基岩版编辑器.md)**
* 修复了在粘贴预览中（实际没有激活剪贴板）将结构移动至剪贴板时不会更新剪贴板客户端小部件的漏洞。
* 修复了体积非常大的手绘选区的一些漏洞。
* 为一些画笔体积的最大尺寸设置了上限，以使其不会超过选择操作的最大体素数量。
* 修复了在编辑器打开期间将下界作为起始维度而进入时会触发大量脚本小部件错误的漏洞。
* 修复了鼠标会在切换至十字准星模式时被释放的漏洞。
* 在视口上加入了一个动画，并且现在会在记录错误时打开日志面板。
* 重组了工具轨道中的核心模态框工具。
* 为3D方块光标加入了指示器，以显示鼠标指向的面。
* 加入了一个新的教程流程，可在帮助菜单中激活。

* *结构**
* 现在古迹废墟会在相同世界种子的Java版和基岩版的原始桦木森林中生成在相同位置上。

# 实验性
本段落的内容需要开启[实验性玩法](实验性玩法.md)中的“”、“”、“即将推出的创作者功能”和“”子选项后才可使用。

# = 生物 =
* *[快乐恶魂](快乐恶魂.md)**
* 现在生命值为，而不是。

# = 游戏内容 =
* ***
* 加入了“”画面选项。
* * 用于调整云的渲染距离。

# = 命令格式 =
* ***
* 用于修改相机预设的控制方案，语法如下：
* *
* ** 清除控制方案。
* *
* ** 设置控制方案。
* 参数：
* * ：指定要被修改相机预设控制方案的玩家。
* * ：指定控制方案。目前可用的控制方案有。

# = 技术性 =
* *方块**
* 为剔除规则模式加入了新字段。其会提供对将要触发面或部分被剔除的方块的更多控制。
* * 支持的值有。
* ** 若相邻方块为完整方块且不透明，则条件。
* *** 所有条件均为的扩展，这意味在检查扩展的条件前，完整且不透明的方块始终会条件。
* ** 若在几何组件中定义，相邻方块与被遮挡的方块共享相同的，则条件。
* *** 若所比较的剔除层中有一个为，则条件永远不会。
* ** 若相邻方块拥有相同的标识符，则条件。
* ** 若相邻方块拥有相同的标识符且排列方式完全相同，则条件。
* * 此功能仅在开启“即将推出的创作者功能”选项后可用。
* 为方块的几何组件加入了新字段。其允许在剔除规则文件中使用条件时将不同方块分组在一起。
* * 使用命名空间时，允许的值只有：。
* * 此功能仅在开启“即将推出的创作者功能”选项后可用。

* *游戏内容**
* 加入了组件，允许自定义生物群系替换掉原版生物群系的一部分。
* * 将此组件添加到行为包中的生物群系文件中即可使用此组件。

* *图形**
* 为所有平台上的模式重新启用了局部曝光。
* 修复了蜡烛、雕纹书架和干海带块在模式和光线追踪模式下不会使用PBR纹理的问题。（）
* 现在染色玻璃在使用时会以正确颜色渲染。

# 修复
* *方块环境音效**
* 现在沙子、枯萎的灌木和陶瓦会播放环境音效。（）

* *方块**
* 现在在蓝冰和熔岩已经再次位于适当的位置后，玄武岩会在最后放置灵魂土时生成。（）

* *游戏内容**
* 更改了鞘翅的行为，现在使用其滑翔时，即使将烟花火箭放置在方块上，烟花火箭也只能用于加快滑翔速度。
* 修复了在如甘蔗等另一方块中为鞘翅使用烟花火箭时会出现来回弹动现象的问题。（）

* *生物**
* 修复了唤魔者行走时其腿部不会移动的问题。（）

* *用户界面**
* 现在在携带版UI下与物品栏选项卡交互时，收纳袋UI会正确消失。
* 修复了离开睡觉屏幕后游戏会在短时间内无响应的漏洞。</onlyinclude>

# 参考

# 导航

[0](Category:基岩版1.21.80预览版.md)
[0](Category:发布于2025年的预览版.md)

[de:Bedrock Edition beta 1.21.80.27](de:Bedrock Edition beta 1.21.80.27.md)
[en:Bedrock Edition Preview 1.21.80.27](en:Bedrock Edition Preview 1.21.80.27.md)
[ja:Bedrock Edition Preview 1.21.80.27](ja:Bedrock Edition Preview 1.21.80.27.md)
[pt:Edição Bedrock Preview 1.21.80.27](pt:Edição Bedrock Preview 1.21.80.27.md)
[ru:Preview 1.21.80.27 (Bedrock Edition)](ru:Preview 1.21.80.27 (Bedrock Edition).md)
[uk:Preview 1.21.80.27 (Bedrock Edition)](uk:Preview 1.21.80.27 (Bedrock Edition).md)